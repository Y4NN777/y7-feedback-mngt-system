# Session Summary — 2026-08-10

## Objective

Turn sprint Day 1 and Day 2 into a Goal-directed execution plan with explicit
validation, then begin execution from the first approved BDD/TDD slice.

## Active Goal

Achieve the cleanest possible verified completion of every Day 1 and Day 2
deliverable and Gate G1/G2 condition. Complete all locally executable work,
attach executable evidence, and keep externally unprovable requirements
explicitly blocked without reducing scope.

## Current state

- Stage 1 is complete: the repository now contains a pinned pnpm workspace,
  strict TypeScript, domain and trusted Function packages, a bilingual
  React/Vite PWA root, Vercel policy, and deterministic quality gates.
- The locally provable portion of Stage 2 is complete: environment contracts,
  startup rejection for trusted configuration, correlation, allowlisted
  operational events, minimized Web Vitals, and synthetic probes are present.
- A public Vercel Preview now passes the deployed SPA deep-link, PWA/shell
  cache, immutable-asset, and security-header probe. Automatic GitHub Preview
  linkage, the controlled product domain, and Production isolation remain.
- The Stage 3 pure-domain matrix now enforces immutable Workspace/Project
  ownership, fixed role/assignment decisions, permanent global slug history,
  canonical redirects, and neutral unavailable intake outcomes.
- The Stage 4 local foundation now includes provider-neutral policy, GitHub App
  and GitLab OAuth adapters, a provider-bound encrypted Appwrite grant vault,
  and a deterministic 10 MiB-plus-overhead Function ingress probe.
- Stage 5 is complete: Reporter trust/scope, typed Feedback source, declarative
  Context, and the bilingual WiseMoney intake/review journey pass their local
  exit criteria.
- The Stage 6 local foundation now covers Feedback-specific proof lifecycle,
  Reporter-safe retrieval/actions, idempotent acceptance coordination, real
  Node cryptography, trusted HTTP boundaries, concrete TablesDB persistence,
  and the bilingual accountless return journey.
- The Stage 7 local foundation now covers byte-derived seven-format validation,
  concrete private Appwrite Storage staging, atomic TablesDB metadata
  association, cleanup/reconciliation, exact Attachment ownership/lifecycle,
  and authorized private download policy.
- The Goal plan and `AGENTS.md` engineering contract are approved, and the
  Codex Goal is active.
- The Goal plan retains every Day 1 and Day 2 sprint task and orders work into
  seven dependency stages ending at Gates G1 and G2.
- `TASK-DEL-001`, `TASK-TEST-001`, `TASK-REP-001`, and `TASK-FDB-001` are
  `DONE`; observability and the continuous security/UX tasks are `IN PROGRESS`;
  deployed tasks with unavailable external evidence remain `BLOCKED`.
- Every local contract and currently available adapter slice is complete.
  Gates G1/G2 and their affected parent tasks remain blocked on real
  non-production services; no deployed result is inferred from local adapters.
- pnpm 10.32.1 is pinned and the frozen lockfile install passes.
- Root `AGENTS.md` now governs personal GitHub identity, human-attributed atomic
  commits without machine co-authorship, task branches/pull requests, BDD/TDD,
  coding standards, testing, quality gates, and session evidence.
- SOLID is explicitly enforced across module responsibility, extensibility,
  adapter substitutability, capability-focused ports, dependency direction,
  contract tests, and pull-request review without speculative abstraction.
- The configured origin targets the personal account `Y4NN777`; no credential
  or literal email address was copied into session documentation.
- SSH authentication succeeded as `Y4NN777`, and repository-local Git author
  identity is configured for the personal account.
- Branch `task/GOAL-day-1-day-2` is pushed to the personal remote with the
  complete atomic history through `c199d4b`.
- GitHub reports the two planning commits as `unknown_key`, but an audit of
  Aïobi Messenger proved that its intended convention is human Git authorship,
  no machine `Co-Authored-By`, and no cryptographic signature requirement.
  The signing-key condition was therefore removed as an invented blocker.
- Deployed checks require non-production Appwrite, Vercel, GitHub App, GitLab
  OAuth, antivirus, and authenticated GitHub administration capability that is
  not currently available in the workspace/session.

## Stage 1 evidence

- Red/green TDD: the structural acceptance test first failed on the absent root
  package, then passed after the workspace was created.
- Final local tests: 10 structural/unit/component/Function assertions pass.
- Coverage: 100% statements, branches, functions, and lines for measured
  TypeScript source in `packages/domain`, `functions/api`, and `apps/web`.
- Browser: 6 Playwright scenarios pass in desktop Chrome and at 320 x 720,
  including FR/EN keyboard switching, no catalog enumeration, axe WCAG AA,
  overflow, manifest, and service-worker cache safety.
- Delivery: frozen install, format, lint, typecheck, build, and security scan
  pass; the scan checks 17 source/build/configuration files with zero finding.
- Visual review: the Organic sand/sage/clay interface using Fraunces and
  Epilogue was inspected at desktop and 320 px.

## Stage 2 local evidence

- Red/green TDD was captured separately for the missing configuration parser,
  correlation/telemetry serializer, Web Vital adapter, and synthetic probe.
- Configuration tests cover required/missing fields, invalid/insecure
  endpoints, secret-shaped public keys, environment mismatch, and shared
  authority denial.
- Function telemetry uses one response/event correlation ID and discards every
  field outside the explicit operational allowlist.
- Browser RUM maps only LCP, INP, CLS, value, rating, navigation type,
  environment, and release through a replaceable sink.
- Root/health probes reduce timeout, status, markup, and health-body outcomes to
  bounded pass/fail fields; response bodies never enter results.
- Final local result: 31 tests plus 6 Playwright scenarios pass, measured
  TypeScript coverage is 100%, build and frozen install pass, and 26 scanned
  source/build/configuration files have zero finding.
- Stage 2 commits are `1598aa6` (BDD contract) and `8cc7540` (operational
  boundaries).

## Stage 3 domain evidence

- Red/green TDD captured six initially failing ownership, authorization,
  assignment-removal, slug-collision, redirect, and unavailable-route cases.
- Two-Workspace policy covers protected read/write/search/aggregate/file,
  notification, Realtime, and Project-management decisions for Owners,
  assigned/unassigned/removed Maintainers, and Platform Operators.
- Slugs remain reserved through rename and inactivity; reserved system routes,
  invalid commands, duplicate Project creation, and missing Project rename are
  rejected.
- Final repository result is 39 local tests plus 6 browser scenarios, 100%
  measured TypeScript coverage, a successful build, and 29 scanned files with
  zero finding.
- Stage 3 commits are `0254418` (BDD contract) and `c5eefeb` (domain policy).

## Stage 4 local evidence

- Callback state binds Owner, Workspace, Project, provider, nonce digest, expiry,
  and safe relative return location, and is consumed once.
- Cross-scope, wrong actor/provider/nonce, expired/replayed callback, unsafe
  return, unauthorized/duplicate/cross-provider repository, and invalid
  lifecycle cases fail.
- Active connections expose only selected authorized repository identities;
  suspension/disconnection stop provider use and disconnect invokes opaque
  grant revocation once.
- `pnpm probe:ingress` sends exactly 10 MiB of deterministic file bytes plus
  multipart overhead and reports no request/response content or credential.
- Final repository result is 47 local tests plus 6 browser scenarios, 100%
  measured TypeScript coverage, successful frozen install/build, and 30 scanned
  files with zero finding.
- Stage 4 commits are `b4d969c` (BDD contract), `cf3e92d` (connection policy),
  and `35f9fe5` (ingress probe).
- Concrete GitLab OAuth and GitHub App adapters plus encrypted Appwrite grant
  persistence were added later and are recorded in the closure section.

## Stage 5 evidence

- Reporter tests cover unidentified, voluntary contact, public external ID,
  verified assertion matching, exact scope isolation, attribution history,
  anonymization, malformed claims, and cross-Workspace denial without browser
  fingerprinting or equality-based public merges.
- Feedback tests cover all three system types, required/optional semantic
  fields, inactive/disabled/malformed Project configuration, source/type
  mismatch, immutable category separation, Attachment manifest bounds, and
  declared typed Context trust and validation boundaries.
- At the Stage 5 checkpoint, `/wisemoney` provided a direct, non-enumerated
  Project path and an intentional local review. The closure work later
  connected that review to the trusted intake HTTP gateway.
- Final repository result is 70 local tests plus 10 Playwright scenarios.
  Domain, configuration, and Function coverage is 100%; web coverage is 98.98%
  statements, 96.36% branches, and 100% functions/lines. Frozen install,
  format, lint, typecheck, build, and a 34-file zero-finding security scan pass.
- Axe WCAG AA, keyboard selection, language-state preservation, no horizontal
  overflow, and desktop/320 px Chrome checks pass. Visual inspection confirms
  the Organic sand/sage/clay intake and three-band source/identity/context
  review.
- Stage 5 commits are `a5fad4e` (BDD contract), `1850c4a` (domain contracts),
  and `76a3304` (intake/review journey).

## Stage 6 local evidence

- Feedback-specific proof policy separates the stable reference from a random
  256-bit secret, stores only a verifier, denies missing/invalid/revoked/sibling
  access identically, and preserves Feedback identity through rotation and
  revocation.
- Reporter projection explicitly includes original/current source, lifecycle,
  Reporter-visible history/messages/evidence, revisions, and deletion requests;
  it cannot include Internal Notes, workspace classification, or unrelated
  Feedback. Clarification, revision, and deletion-request actions are scoped,
  validated, ordered, and attributable.
- The trusted coordinator prepares Reporter, Feedback, `received` lifecycle,
  reference/verifier, notification, outbox, and idempotency result as one
  transaction input. Exact retry reopens an authenticated AES-256-GCM envelope;
  payload conflict or read/envelope/commit failure returns no success material.
- The real Node adapter generates a URL-safe proof, hashes with SHA-256, compares
  in constant time, seals retry material with AES-256-GCM, and rejects tampering,
  malformed data, wrong keys, and invalid sizes.
- `/retrieve` keeps proof material only in controlled memory state and never in
  the URL. FR/EN state preservation, uniform denial, distinct retryable state,
  allowlisted result rendering, keyboard, axe, and 320 px behavior pass. The
  HTTP gateway reports honest retryable unavailability when the trusted API
  cannot be reached. The accepted-material component explains secure proof
  preservation only after the server confirms acceptance.
- Final repository result is 101 local tests plus 14 Playwright scenarios.
  Domain, configuration, and Function coverage is 100%; web coverage is 99.31%
  statements, 97.43% branches, and 100% functions/lines. Frozen install,
  format, lint, typecheck, build, and a 41-file zero-finding scan pass.
- Stage 6 commits are `090ea8e` (BDD contract), `40a8042` (domain access),
  `e794643` (intake and crypto), `61a76e2` (trusted access boundary), and
  `b87e999` (accountless web journey).
- `TASK-ACC-001` and `TASK-INTAKE-001` remain blocked until the concrete local
  trusted endpoint and TablesDB adapters pass against non-production Appwrite
  with rollback and uniqueness evidence.

## Stage 7 local evidence

- The trusted validator derives JPEG, PNG, WebP, GIF, PDF, UTF-8 TXT, and CSV
  from structurally parsed bytes, derives SHA-256 and safe metadata, invokes an
  antivirus port, and ignores client MIME/extension for acceptance.
- Spoofing, malformed structures, archives, executables, polyglots, malware,
  binary/invalid UTF-8 text, executable text, scanner failure, oversize input,
  and a sixth file fail closed.
- The acceptance saga stages one to five operation-owned objects with explicit
  private visibility and commits their Feedback-bound manifest only after all
  validation passes. Any staging, validation, identifier, cleanup, or metadata
  commit failure returns no success material.
- Cleanup covers every intended object. Repeated sweeping removes stale
  unassociated objects, retains committed objects, contains isolated failures,
  and recovers an earlier cleanup failure.
- Attachment policy binds Feedback, Workspace, Project, source entry, audience,
  private object, and lifecycle. Trusted download reads bytes only after exact
  Reporter or current workspace authorization; unknown, sibling, public,
  removed, cross-scope, soft-deleted, and purged cases share one denial.
- The original Stage 7 checkpoint passed 143 local tests plus 14 Playwright
  scenarios; the later concrete adapters are recorded below.
- Stage 7 commits are `288485b` (BDD contract), `cfe7648` (content validator),
  `6516bc6` (domain ownership/lifecycle), `8ed2b92` (private saga), and
  `9afc68e` (authorized download).
- `TASK-ATT-002` and `TASK-ATT-003` remain blocked until the concrete local
  Storage and TablesDB adapters pass through real Appwrite Function ingress,
  antivirus, authenticated authorization, cleanup, and lifecycle execution.

## Concrete adapter and delivery closure

- CI now runs the full frozen-install, format, lint, typecheck, test, coverage,
  build, Playwright, and security gate with pinned actions and pnpm-managed
  Chromium (`8e3c05a`, `ac7cce9`).
- Trusted intake and accountless access now traverse concrete strict Appwrite
  project, transaction, grant, and projection adapters; the web review and
  retrieval journeys use real HTTP gateways (`a08c7ac` through `fac0c1e`).
- GitLab OAuth and GitHub App adapters enumerate only provider-authorized
  repository identities and revoke remotely before local grant removal
  (`621a0bb`, `5afea09`).
- Provider grants are encrypted with provider/reference-bound AES-256-GCM in a
  private Appwrite table, using a key required to differ from the Access Proof
  key (`c199d4b`).
- Attachment bytes stage in a private bucket before an operation-owned staging
  row; index failure compensates the uploaded file. Accepted metadata commits
  atomically only after exact staging ownership and is parsed strictly for
  authorized download (`07cd781`, `0ef4c21`, `cf88ed2`).
- Latest local evidence: 214 tests pass (11 repository, 16 configuration, 46
  domain, 122 Function, 19 web); domain/configuration/Function coverage is
  100%; web coverage is 96.65% statements and 95.14% branches; 14 Playwright
  scenarios pass; build succeeds; 58 files scan with zero findings.
- PR #1 was merged into `main` as `c70ea61` on 2026-08-11. The later Appwrite
  infrastructure work is intentionally isolated on
  `task/TASK-DEL-002-appwrite-preview`; it is pushed to `origin` but has no
  pull request yet. Repository-rules configuration and the remaining
  external-service evidence are tracked separately from the local CI result.

### Pull-request Browser gate correction — 2026-08-11

- PR #1 exposed a clean-runner dependency-order defect: Playwright built the
  web package before the runtime export of `@y7-feedback/config` existed.
- `BDD-CI-002` first failed against the old Playwright command, then passed
  after the E2E server command was changed to build the configuration package
  before the web application (`040b7c7`).
- Format, lint, typecheck, all local tests, build, and the 58-file security scan
  pass; the corrected Playwright command passes all 14 browser scenarios.

### Vercel clean-workspace build correction — 2026-08-12

- The Browser-gate failure exposed the same dependency-order risk in
  `vercel.json`: a clean Vercel checkout attempted to build the web workspace
  without first emitting the runtime exports of `@y7-feedback/config`.
- `BDD-VER-002` failed against the original command, then passed after Vercel
  was made to build config before web (`e89c92a`).
- The exact config build, web production build, format check, lint, typecheck,
  and complete local test suite pass. The commit is pushed to PR #1.
- Adapter audit found that environment IDs exist but no repository artifact yet
  declares the TablesDB columns, query indexes, uniqueness constraints, and
  private bucket policy required for reproducible non-production evidence.

### Appwrite preview and reproducible infrastructure — 2026-08-12

- A real `Y7 Feedback Preview` Appwrite Cloud project now exists in Frankfurt
  with public ID `6a7be5dc003be4642e6e`, endpoint
  `https://fra.cloud.appwrite.io/v1`, and a `localhost` Web platform.
- `03016dd` declares 11 private TablesDB tables with adapter-required columns,
  query/uniqueness indexes, encrypted non-queryable sensitive fields, and a
  private encrypted antivirus bucket capped at 10 MiB.
- `3a42f2f` adds an idempotent drift-detecting provisioner and a guarded
  `pnpm provision:appwrite` command. It inspects every existing resource before
  creating absent resources, refuses incompatible state without corrective
  mutation, and redacts unexpected SDK errors.
- `2226d10` corrects server-side relative imports for Node ESM. The complete
  format, lint, typecheck, test, and build gates pass; the compiled provisioner
  starts and rejects execution without its explicit apply path instead of
  failing module resolution.
- A least-privilege local API key was later configured in the ignored
  `.env.appwrite-preview`. No key value is recorded in Git, the plan, the
  summary, or the raw-session curation.

### Appwrite adapter coverage correction — 2026-08-23

- The first CI run on the Appwrite preview branch exposed untested SDK metadata
  validation and transaction rollback paths; the production implementation was
  not weakened and the 100% Function coverage threshold was retained.
- `a79195b` covers every supported Appwrite column/index translation, malformed
  SDK resource metadata, private bucket creation, invalid transaction results,
  fixture drift, failed row/commit rollback, and preservation of the originating
  failure when rollback also fails.
- Local evidence passes: format, lint, typecheck, tests, 100% Function statement,
  branch, function, and line coverage, build, all 14 Playwright scenarios, and
  a 64-file security scan with zero findings.
- The commit is pushed to `task/TASK-DEL-002-appwrite-preview`. GitHub CI run
  `32655459903` completed successfully for both the Quality and Browser gates;
  the branch still has no pull request.

### Real Appwrite provisioning and encryption blocker — 2026-08-23

- The first remote attempt failed before table creation because Appwrite
  requires index `attributes` in bulk-create requests while returning index
  `columns` in table metadata. `4bd3930` translates this asymmetric SDK
  contract and preserves domain terminology.
- After the fix, the preview run succeeded with infrastructure `created: 17`,
  `verified: 1` and fixtures `created: 7`, `verified: 0`: 16 private tables, the
  private encrypted antivirus bucket, and seven deterministic G1 rows now
  exist in the preview project.
- The required replay failed closed with
  `APPWRITE_INFRASTRUCTURE_DRIFT:table:reporters`. Appwrite metadata reports
  representative sensitive `varchar` and `text` columns as unencrypted even
  though the manifest requested encryption. Appwrite documents database-column
  encryption as requiring a Pro plan or higher.
- The nonconforming preview resources are not treated as G1 evidence. No
  deletion or weakening was performed. TablesDB does not expose encryption as
  an updateable property, so safe recovery requires encryption capability and
  explicit approval to recreate the generated non-production schema.

## Session files

- Plan: `docs/sessions/plans/2026-08-10-day-1-day-2.md`
- Repository engineering instructions: `AGENTS.md`
- Raw Codex snapshot (local, Git-ignored):
  `docs/sessions/raw/rollout-2026-08-10T09-58-41-019feb1c-335e-7913-91a4-ddbe150cfb7b.jsonl`
- Summary: `docs/sessions/2026-08-10-summary.md`

## Next action

Continue the real G1/G2 actor, scope-isolation, and deployed-service matrices.
Open a pull request from `task/TASK-DEL-002-appwrite-preview` into `main` and
activate the `main` ruleset. Vercel, telemetry, provider, and remaining
operational evidence is still required; no gate may be marked complete from
partial remote results.

## Raw-session correction

The previously hand-authored JSONL was removed. The raw artifact is now a
byte-for-byte snapshot of the Codex-generated rollout for this session. It is
never manually appended or committed; this summary is the curated record.

## Accepted Education-plan encryption design — 2026-08-23

The operator selected the recommended application-level encryption design.
`ADR-004` records contextual AES-256-GCM envelopes, a rotation-ready keyring,
and strict separation from Access Proof and provider-grant keys. Commit
`70bdd48` supplies the validated envelope primitive. The active adapter slice
integrates it with intake, accountless access, and Attachment metadata, and
aligns the TablesDB manifest with the Education project's observed column
capabilities. Local lint, typecheck, and all tests pass after integration.

No remote resource was deleted or changed by this design work.

## Appwrite reprovision evidence — 2026-08-23

With explicit operator authorization, the 16 manifest-owned preview tables and
their seven synthetic fixtures were deleted and recreated. The database and
private bucket were preserved. The creation pass returned infrastructure
`created: 16`, `verified: 2` and fixtures `created: 7`, `verified: 0`.

After canonicalizing Appwrite's omitted `encrypt: false` default and equivalent
UTC `+00:00` fixture timestamps, the mandatory replay passed with infrastructure
`created: 0`, `verified: 18` and fixtures `created: 0`, `verified: 7`. Local
format, lint, typecheck, tests, 100% coverage, build, 14 browser scenarios, and
the 65-file zero-finding security scan pass. No secret value was printed or
added to Git.

## Real G1 intake evidence — 2026-08-24

Draft PR `#2` is open and its `Quality gate` and `Browser gate` checks pass. A
new isolated real-service matrix passes against preview Appwrite: acceptance,
exact replay, payload conflict, invalid-proof denial, authorized accountless
retrieval, proof rotation, denial of the old proof, authorization with the
rotated proof, revocation, denial of the revoked proof,
application-encrypted persistence, and deletion of all seven synthetic rows.
No proof or sensitive row content is printed.

The matrix found and drove fixes for runtime domain package exports and
undeclared lifecycle/notification `id` attributes in TablesDB payloads. The
complete local gate passes with 100% coverage, 14 browser scenarios, and a
67-file security scan with zero findings. At that checkpoint, rollback and
outbox consumption were the next Appwrite evidence slice; the section below
records their subsequent completion.

## Real rollback and durable outbox evidence — 2026-08-24

The G1 verifier now injects a deterministic failure before the third write of a
real Appwrite transaction. The trusted API returns retryable, all seven target
rows are absent after rollback, and defensive cleanup finds no partial row.

A capability-focused durable outbox worker and Appwrite adapter now provide
transactional claims, expiring leases, encrypted delivery state, bounded
attempts, retry scheduling, permanent terminal failure, and provider
idempotency by Notification ID. Safe logs contain only event, channel, attempt,
and outcome. The real Preview matrix proves retry, recovery to success,
permanent failure, a final idle/deduplicated pass, encrypted terminal rows, and
cleanup of all fourteen synthetic rows. The deterministic sender proves outbox
orchestration and TablesDB persistence; external notification delivery remains
blocked pending a real non-production provider.

Final local validation passes with the frozen pnpm lockfile, formatting, lint,
strict typecheck, all tests, 100% coverage, production build, all 14 Playwright
scenarios, and a 70-file security scan with zero findings.

The first GitHub run for `1ac92d1` failed at `Test` while earlier quality steps
passed and Browser was consequently skipped. The clean runner had no emitted
`config` or `domain` files, unlike the local workspace after real-service
validation. Root tests and coverage now build their runtime dependencies before
loading API suites. `BDD-CI-006` prevents regression, and an explicit local run
with both ignored `dist` directories initially absent passes.

## Real private Attachment evidence — 2026-08-25

The Attachment proof was separated from the longer G1 verifier into the
dedicated `pnpm verify:appwrite:g2:attachments` command. Against the real
non-production Preview project, it creates a synthetic parent Feedback, stages
one strict UTF-8 text Attachment in the private bucket, and verifies that the
Appwrite file has no direct permissions. It also verifies an encrypted display
name in TablesDB, byte-for-byte Reporter download, and the same denial result
for a sibling Feedback before removing the object, staging row, metadata row,
and seven parent-intake rows.

The first real attempt exposed Appwrite's equivalent `+00:00` datetime
representation in staging and metadata reads. The adapters now normalize that
representation to the canonical UTC `Z` boundary while continuing to reject
non-UTC offsets and invalid instants. The final matrix returned all eight safe
success indicators, including `removedObject=true` and `cleanedRows=8`. A
separate read-only audit counted synthetic G2 prefixes across eight relevant
tables and found `residualRows=0`.

The deterministic clean scanner used by this matrix proves orchestration of the
malware port; it is not external antivirus evidence. Likewise, direct Node SDK
execution does not prove deployed Appwrite Function ingress. `TASK-ATT-002` and
`TASK-ATT-003` therefore remain `BLOCKED` for the remaining external and
deployed matrices recorded in the Goal ledger.

At this checkpoint, frozen pnpm install, formatting, lint, strict typecheck,
all repository tests and coverage suites, production build, all 14 Playwright
scenarios, and the 72-file security scan pass. The Function package has 205
passing tests and retains 100% statement, branch, function, and line coverage.

### Attachment PR clean-runner correction — 2026-08-25

GitHub accepted `4f3889c`: both push and pull-request Quality gates passed, but
both Browser gates failed while starting the Playwright web server. A temporary
archive of that exact commit with a fresh offline pnpm install reproduced the
failure: the E2E command emitted the configuration runtime but not
`@y7-feedback/domain`, so Vite could not resolve the domain import used by the
intake screen.

`BDD-CI-002` was made red against that incomplete command. The Playwright server
now builds both runtime workspace dependencies before the web package. The
policy test passes, and the exact Browser gate in the same clean archive passes
all 14 desktop and 320 px scenarios. No product behavior or acceptance evidence
was weakened.

### Real stale-stage sweeper evidence — 2026-08-25

The dedicated G2 verifier now stages two uniquely named private objects at an
isolated historical instant and atomically associates exactly one of them. A
mandatory read-only preflight requires the sweep cutoff to select exactly those
two objects; the matrix refuses to invoke the destructive sweep if any unrelated
staging row is present.

Against Appwrite Preview, the real sweeper examined two objects, removed the
unassociated orphan, retained the associated object, and reported no contained
failure. The matrix then proved that the retained file remained private, its
staging and metadata association remained present, and both the orphan staging
row and Storage file were absent. Final compensation removed the retained test
object and its metadata. A separate eight-table read-only audit returned
`residualRows=0`.

Five BDD scenarios cover the successful proof, unsafe selection, retryable and
incomplete sweeps, invalid artifacts, cleanup failures, and unexpected lookup
errors. The Function package now has 210 passing tests and 100% statement,
branch, function, and line coverage. This closes the direct Preview sweeper
evidence only; deployed lifecycle transitions and deployed authorization remain
required before `TASK-ATT-003` can leave `BLOCKED`.

### Preview Function deployment — 2026-08-25

The Preview API is now declared by `appwrite.config.json` without variables or
secrets. Its dynamic execution key has only `rows.read` and `rows.write`; the
public execution boundary remains protected by the application's Project and
proof policies. Runtime startup maps Appwrite's injected endpoint, Project, and
`x-appwrite-key` into the existing strict server configuration and rejects
missing, duplicate, or contradictory authority.

The project runtime catalog exposed Node 22, 25, and 26, but not Node 24. An
initial Node 24 push failed before Function creation. The corrected Node 22 LTS
manifest created and activated deployment `6a8ce80f76c3ccb8e218`. Remote logs
prove a pnpm 10.32.1 frozen install and successful builds of the configuration,
domain, and API workspaces. No secret was synchronized by that deployment.

After explicit operator authorization, the idempotent variable provisioner
created exactly 25 secret variables and excluded the static Appwrite API key,
endpoint, and Project ID. Its mandatory replay created none and updated all 25
without duplication. An authenticated SDK execution and an independent public
execution without an API key both completed `GET /health` with HTTP 200 and
`{ "status": "ok" }`; no secret value or execution key was printed.

The CLI skipped default-domain creation because no Appwrite Console session is
active, and the deterministic domain returns `router_rule_not_found`.
Direct-domain and 10 MiB ingress evidence remain blocked; public execution
evidence is recorded below.

Local validation passes frozen install, formatting, lint, strict typecheck,
217 Function tests, all repository tests and coverage, production build, 14
Playwright scenarios, and a 76-file security scan with zero findings.

### Public deployed intake and retrieval — 2026-08-25

The new `pnpm verify:appwrite:deployed:g1` command invokes the active Function
through Appwrite's public execution API without an API key. The final real run
proved unavailable-Project and invalid-input denial, acceptance, exact replay,
payload conflict, indistinguishable reference-only and invalid-proof denial,
authorized Reporter retrieval, application-encrypted persisted values, and
cleanup of all seven synthetic rows. A separate read-only audit of the seven
tables returned `residualRows=0`. No reference or Access Proof was printed.

The first run passed all eight Function requests but exposed a cleanup-only
assumption that Feedback stored the human reference. It intentionally does not:
the reference belongs to Grant, Notification, and Idempotency. A strict
two-second preflight identified exactly the seven rows created by that run,
validated their complete relationship chain, and removed them. The corrected
verifier begins with the deterministic Idempotency row and follows authoritative
relations, preventing recurrence.

This closes public Appwrite execution evidence for intake and retrieval. It does
not claim a Function-domain path, the 10 MiB multipart result, deployed
rotation/revocation endpoints, or external notification delivery; those remain
explicit blockers.

The complete local gate passes with 221 Function tests, 100% Function coverage,
14 Playwright scenarios, and a 78-file security scan with zero findings.

### Direct Function-domain ingress — 2026-08-25

The Appwrite-managed Function domain is active and `GET /health` returns HTTP
200. Outside-in BDD/TDD added a Preview-only ingress route that validates the
binary body actually received, denies inconsistent or missing size evidence,
is absent in Production, and does not invoke Appwrite's JSON parser for
multipart requests. The real probe initially identified lowercase domain-method
handling, lazy multipart-to-JSON parsing, and incorrectly provisioned
`development` labels in the Preview Function. These were corrected without
printing or committing secrets, the 25-variable set was replayed idempotently,
and the Function was redeployed.

The deployed result accepts exactly 10,485,760 file bytes and 10,485,931 total
multipart bytes with HTTP 200. This supplies the previously missing Function
ingress-size evidence. PR #2 merged the proof into `main` as `45ee705`, so
`TASK-ATT-001` is now `DONE`. External antivirus and the other deployed G1/G2
boundaries remain blocked and are not inferred from this result.

### Direct-domain intake and retrieval — 2026-08-25

The same deployed G1 matrix can now run through the managed Function domain via
a strict HTTPS adapter rather than Appwrite's execution API. Sixteen adapter
examples cover exact JSON forwarding, empty requests, unsafe origins, methods
and paths, malformed responses, and non-disclosing network failure. The real
domain run proved Project and validation denial, acceptance, exact replay,
payload conflict, reference-only and invalid-proof denial, authorized
retrieval, proof rotation, old-proof denial, rotated-proof retrieval,
revocation, revoked-proof denial, encrypted persistence, and cleanup of exactly
seven rows.

This establishes the complete deployed access-proof behavior matrix without
weakening or duplicating the earlier matrix. `TASK-ACC-001` now awaits merge;
real external notification delivery remains an explicit blocker for intake.

### Public Vercel Preview — 2026-08-25

The linked `y7-feedback` Vercel project now has a public Preview deployment
`dpl_ARivdqCDfF3Dbke4TjMrYzbjtz8Q` at
`https://y7-feedback-lb5nynkjj-y4nn-projects.vercel.app`. Deployment protection
was disabled with explicit user authorization because the unidentified
Reporter journey must be publicly reachable.

BDD/TDD added `pnpm probe:vercel:preview`, covering Vercel-auth interception,
root/deep-link equivalence, revalidated shell and PWA files, immutable hashed
assets, required security headers, and deployed `/api/*` denial. The real probe
passed all checks. The fallback now reserves `/api/*` instead of returning the
SPA shell for API-like paths. The complete local gate passes, including 14
Playwright scenarios and a 79-file security scan with zero findings.

At the time of this first manual deployment, automatic task-branch Preview was
still unavailable. The later Git-linked proof below supersedes that limitation.
The controlled product domain and Production separation remain unproved, so
`TASK-VER-001` stays `IN PROGRESS` and `TASK-DEL-002` stays `BLOCKED`.

### Deployed Reporter Attachment authorization — 2026-08-25

The Preview Function now has the minimal additional `files.read` scope and a
proof-authorized binary Attachment route. Authorization derives Feedback scope
from the accountless grant before reading metadata or Storage. Responses are
`no-store`, use an RFC 5987 filename, and retain the validated media type and
exact byte length. Malformed or unsafe binary responses fail closed.

The first deployed comparison isolated a runtime-boundary defect: Appwrite's
`res.binary` required an explicit Node `Buffer`, although the local structural
type also accepted `Uint8Array`. After correction and redeployment, the real
matrix created two independent valid Access Proofs, downloaded the first
Feedback's private TXT bytes, denied the same Attachment with the sibling
proof, and cleaned seven sibling rows, eight parent/Attachment rows, and the
isolated sweeper fixture. No proof or file content was printed.

The API now passes 269 tests at 100% statement/branch/function/line coverage;
14 Playwright scenarios and the 80-file security scan pass. `TASK-ATT-003` is
`IN PROGRESS`: deployed lifecycle transitions and real workspace assignment
removal authorization remain required.

### Real Attachment lifecycle — 2026-08-25

BDD/TDD added the Workspace-scoped lifecycle policy, trusted coordinator, and
transactional Appwrite compare-and-set adapter for `available`,
`soft_deleted`, and `purged`. The adapter rolls back stale or malformed
transitions and exposes only stable retryable failures. Purge records the
irreversible state before deleting the private object and is safe to retry.

The Preview matrix mutated real Appwrite metadata and observed the result
through the deployed Function domain: soft-delete immediately denied Reporter
download, restore returned the original bytes, purge denied download and
removed the private Storage object, and a repeated purge remained successful.
All parent, sibling, Attachment, and sweeper fixtures were cleaned. The bounded
output reported:

```text
deployedSoftDeleteHidden=true deployedRestoreAuthorized=true
deployedPurgeHidden=true deployedPurgeRemoved=true
removedObject=true cleanedRows=8
```

The domain package passes 47 tests and the API package 284 tests with 100%
statement, branch, function, and line coverage. Format, lint, typecheck, full
tests, coverage, build, all 14 Playwright scenarios, and the 82-file
zero-finding security scan pass. `TASK-ATT-003` stays `IN PROGRESS`: real
Auth-backed Workspace assignment and removal authorization remains the final
task-specific evidence gap.

### Real Appwrite Auth-backed Workspace Attachment — 2026-08-25

The selected design validates a bearer JWT with Appwrite `Account.get`, derives
the principal only from that result, and reloads the authoritative Project,
Workspace membership, and Project assignment using the Function's private
server capability on every request. Reserved Appwrite headers are not trusted,
no role result is cached, and no parallel application session token was added.

Deployment `6a8d14c47f2dc24f0e80` added the protected Workspace Attachment route.
The real Preview matrix created two temporary Auth Users and one private
Internal Note Attachment. It proved Owner access, unassigned Maintainer denial,
assigned Maintainer access, forged-principal and cross-scope denial, immediate
assignment-removal and membership-removal denial, and JWT denial after its
Appwrite session was deleted. Post-cleanup rereads proved absence of both Auth
Users, all three role rows, Attachment/staging rows, and the Storage file.

The bounded evidence reported every `workspaceAuth` condition as `true`,
including `cleaned=true`. `TASK-AUTH-001` moves to `IN PROGRESS`, and
`TASK-ATT-003` remains `IN PROGRESS`; their task-specific real matrices pass,
but merge and final task gates are still required before `DONE`.

## Deployed Project routing evidence — 2026-08-25

Outside-in BDD/TDD replaced the web-only `/wisemoney` special case with a
capability-focused public Project resolver. The trusted Function resolves the
private global slug reservation first, reloads the exact Project by its
authoritative ID, and returns only a current projection, canonical redirect, or
one neutral unavailable outcome. Inactive, missing, malformed, and inconsistent
records fail closed without a Project catalog.

Function deployment `6a8d1c3a1d77c626f7b3` proved the real Preview fixture:
`wisemoney` returned the active bilingual Project purpose,
`wisemoney-legacy` returned only canonical slug `wisemoney`, and an unknown
slug returned the same HTTP 404 unavailable outcome. All responses were
`no-store`. An explicit OPTIONS request returned HTTP 204 with the bounded
public CORS contract. GET routing no longer touches Appwrite's absent JSON-body
getter, removing the previously observed HTTP 500.

The Vercel project previously had no persisted environment variables, causing
the production bundle to reject startup before rendering. Six public-only
`VITE_*` Preview values are now configured globally for Preview branches; no
secret was added. The CSP explicitly permits the managed
`*.appwrite.network` Function domain. Deployment
`dpl_GfHQNCokvc81gmi3JSGc1WTaCjyx` is `READY` at:

```text
https://y7-feedback-elj3q9xvo-y4nn-projects.vercel.app
```

Real headless-browser evidence proved the current intake route, client-side
history replacement from `/wisemoney-legacy` to `/wisemoney`, one neutral
unknown screen in French and English, and no horizontal overflow at 320 px.
The local browser matrix now passes 18 desktop/mobile scenarios. This advances
`TASK-PROJ-001` but does not mark it `DONE`: its `TASK-OWN-001` and
`TASK-VER-001` dependency gates and merge requirement remain open.

Implementation commit `265a90c` is pushed on
`task/TASK-ACC-001-direct-domain`. GitHub Actions run `32828622738` completed
both the Quality gate and Browser gate successfully. No pull request currently
targets `main`, so merge evidence remains absent rather than inferred.

### Real direct Appwrite isolation — 2026-08-25

The Preview verifier now uses two temporary Auth principals to disprove direct
SDK bypass across Projects, Feedback, Notifications, Source Connections,
Provider Grants, and Attachment Storage. Both principals saw empty list
projections, were denied known sentinels, and were denied direct create, update,
and delete mutations. A permission-scoped third principal positively received a
Realtime control event while both tested principals received none on the same
row channel.

The successful bounded result reported two principals, six surfaces, and true
list, sentinel, mutation, Realtime, and cleanup conditions. An earlier Node
heartbeat incompatibility interrupted one run. Its single fixture set was
identified by exact shared suffix, IDs, names, and payload markers before only
those 3 principals, 9 rows, and 1 file were removed. A final read-only audit
reported `[0,0,0,0]` table counts, zero files, and zero principals.

Vercel project configuration independently confirms `ssoProtection=null`, and
the current Preview URL returns anonymous HTTP 200. No repository secret or
temporary recovery script was retained. `TASK-OWN-001` remains `IN PROGRESS`
until every protected Function operation has equivalent cross-scope evidence
and the slice is merged.

### Automatic Git-linked Vercel Preview — 2026-08-25

The push of `56cf05d` triggered the first observed Git-linked Preview, exposing
a clean-clone build gap: Vercel compiled Config and Web but not Web's compiled
Domain dependency. `BDD-VER-002` reproduced the gap red-first. Commit `3a5ebbc`
now builds Config, Domain, then Web.

The corrective push automatically created a `READY` Preview from
`task/TASK-ACC-001-direct-domain` at
`https://y7-feedback-jpgpgqvm7-y4nn-projects.vercel.app`. Its public probe
passed anonymous access, deep-link fallback, revalidated shell/PWA caching,
immutable assets, security headers, and reserved `/api` denial. GitHub Actions
run `32831796557` completed both Quality and Browser gates successfully.

This closes the automatic task-branch Preview and public Reporter reachability
gaps. A controlled product domain and separate Production environment with
cross-environment denial remain required before `TASK-VER-001` or
`TASK-DEL-002` can be `DONE`.

### Consolidation Draft PR — 2026-08-25

Draft PR [#3](https://github.com/Y4NN777/y7-feedback-mngt-system/pull/3)
targets `main` from `task/TASK-ACC-001-direct-domain` and contains 13 atomic
commits. Its description records the behavior matrices, real Appwrite/Vercel
evidence, rollback strategy, and every remaining external blocker. GitHub
reports the branch mergeable.

Pull-request run `32837082485` completed both Quality and Browser gates
successfully, and the Vercel Preview Comments check succeeded. The PR remains
Draft and no task is marked `DONE` before review and merge.
