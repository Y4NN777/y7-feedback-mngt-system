# Goal Plan — Day 1 and Day 2

## Goal

Achieve the cleanest possible verified completion of every Day 1 and Day 2
deliverable and Gate G1/G2 condition in `docs/09_ONE_WEEK_SPRINT_PLAN.md`.

This means:

1. implement all locally executable work in dependency order;
2. validate every completed result with executable or operational evidence;
3. use real non-production services wherever configuration is available;
4. preserve the complete documented scope;
5. record an item as `BLOCKED` when external evidence is impossible, never as
   complete or silently deferred.

## Mandatory repository instructions

Execution must follow [`AGENTS.md`](../../../AGENTS.md). It defines the personal
GitHub identity and remote, human-attributed atomic commits without machine
co-authorship, branch/pull-request policy, BDD/TDD cycle, coding standards, test
strategy, quality gates, and evidence protocol.

No development starts until its pre-development approval checklist is accepted.

## Goal completion

The Goal is complete only when both gates pass.

### Gate G1

- Two Workspaces and Projects exist in non-production.
- Cross-Workspace CRUD, search, aggregate, file, notification, Realtime, and
  provider access is denied.
- Root, current slug, historical redirect, and unavailable routes pass in
  French and English.
- Preview contains no production or provider secret.
- Provider callbacks cannot cross Workspace or Project scope.
- The Appwrite 10 MB Function-ingress result is recorded.

### Gate G2

- Unidentified and externally identified Reporters submit and retrieve.
- Reference-only retrieval is denied without existence disclosure.
- JPEG, PNG, WebP, GIF, PDF, TXT, and CSV pass actual-content validation.
- Spoofed, polyglot, malware, archive, executable, oversized, binary-text, and
  sixth-file cases fail.
- Any file or commit failure returns no Feedback success and leaves no durable
  orphan.

## Completion rules for every task

A task can move to `DONE` only when:

- its implementation is reviewable and deployable;
- its positive, negative, isolation, retry, and failure paths pass;
- trusted Functions enforce scope for protected behavior;
- affected UI passes FR/EN, keyboard, accessible-name, non-color, and 320 px
  checks;
- logs, caches, responses, and built assets contain no prohibited data;
- install, typecheck, tests, and build pass;
- deployed evidence is attached when the task depends on Appwrite, Vercel, or a
  provider.

## Goal progress ledger

This ledger retains every Day 1 and Day 2 task from the sprint plan.

### Day 1

| Task | State | Required result |
| --- | --- | --- |
| TASK-DEL-001 | DONE | Reproducible React/TypeScript/Vite PWA and modular Appwrite Function workspace. |
| TASK-DEL-002 | BLOCKED | Separate preview and production Appwrite/Vercel configuration with cross-environment denial. |
| TASK-TEST-001 | DONE | Unit, integration, contract, and browser E2E harnesses with deterministic fixtures. |
| TASK-OBS-001 | IN PROGRESS | Correlation IDs, redacted logs, metrics, traces, RUM, and probes. |
| TASK-VER-001 | IN PROGRESS | Automatic task-branch Preview, public access, SPA fallback, safe caching, security headers, and deployed deep links pass; controlled product domain and final Preview/Production isolation remain. |
| TASK-OWN-001 | IN PROGRESS | Real Auth principals cannot directly list/read/create/update/delete or receive Realtime events from private TablesDB/Storage surfaces; the complete protected Function-operation matrix and merge remain. |
| TASK-PROJ-001 | IN PROGRESS | Domain collision/non-reassignment and deployed current/historical/unavailable routes pass against real Appwrite/Vercel in FR/EN and at 320 px; final OWN/VER dependency gates and merge remain. |
| TASK-AUTH-001 | DONE | Real Appwrite JWT-to-principal mapping, Owner and assigned-Maintainer authorization, unassigned/removed/forged/cross-scope denial, session revocation, and zero-residue cleanup passed and merged by PR #3. |
| TASK-SRC-001 | BLOCKED | Local GitHub/GitLab adapters and encrypted Appwrite grant vault pass; real callbacks, selection, and revocation evidence remain. |
| TASK-ATT-001 | DONE | Exact 10 MiB plus multipart overhead passes through the deployed Appwrite Function domain and was merged by PR #2. |
| TASK-SEC-001 | IN PROGRESS | Continuous authorization, isolation, secret, error, cache, and log regression. |
| TASK-UX-001 | IN PROGRESS | Continuous FR/EN, keyboard, assistive semantics, non-color, language safety, and 320 px regression. |

### Day 2

| Task | State | Required result |
| --- | --- | --- |
| TASK-REP-001 | DONE | All Reporter attribution modes without cross-scope merging or fingerprinting. |
| TASK-FDB-001 | DONE | Bug, Suggestion, Review, source preservation, and intentional Context review. |
| TASK-ACC-001 | DONE | Real and deployed direct-domain denial, retrieval, rotation, old-proof denial, rotated-proof retrieval, revocation, revoked-proof denial, cleanup, CI, and merge passed through PR #3. |
| TASK-INTAKE-001 | BLOCKED | Real non-production acceptance, replay, conflict, envelopes, rollback atomicity, durable outbox retry/recovery/terminal state/deduplication, and cleanup pass; public execution and direct-domain acceptance/replay/conflict pass, while real notification delivery remains. |
| TASK-ATT-002 | BLOCKED | Real Preview private Storage staging, private file permissions, TXT validation, compensating removal, and zero-residue audit pass; seven formats and deployed ingress pass, while external antivirus evidence remains. |
| TASK-ATT-003 | DONE | Real Preview encrypted metadata, deployed Reporter and Auth-backed Workspace binary download, distinct-proof sibling denial, Owner/Maintainer assignment and removal checks, transactional soft-delete/restore/purge, physical purge, private caching, isolated sweep/reconciliation, deterministic cleanup, CI, and merge passed through PR #3. |
| TASK-SEC-001 | IN PROGRESS | Day 2 proof, intake, file, error, and data-leak regression. |
| TASK-UX-001 | IN PROGRESS | Day 2 FR/EN intake/retrieval, accessibility, and 320 px regression. |

## Execution path

The stages are sequential. Within a stage, tests and evidence are part of the
implementation rather than a later cleanup activity.

### Stage 1 — delivery spine (`TASK-DEL-001`, `TASK-TEST-001`)

Create:

- `apps/web`, `packages/domain`, and `functions/api` workspaces;
- strict TypeScript and reproducible dependency lock;
- React/Vite PWA shell and Vercel SPA/cache policy;
- Vitest, Testing Library, API-contract, and Playwright harnesses;
- one positive and one fail-closed test in each active layer.

Validate:

```bash
pnpm install --frozen-lockfile
pnpm typecheck
pnpm test
pnpm build
```

Exit: all four commands pass from the repository root and the client bundle
contains no secret-like value.

### Stage 2 — environment and observability (`TASK-DEL-002`, `TASK-OBS-001`, `TASK-VER-001`)

Create:

- typed public/server environment contracts and environment-identity checks;
- startup rejection for missing, mixed, or secret-bearing public config;
- correlation IDs, structured redaction, metrics, traces, Web Vitals, and
  availability probes;
- deployed preview smoke for SPA fallback and cache headers.

Validate:

- configuration unit and contract tests;
- built-asset and log prohibited-data scans;
- preview-to-production cross-environment denial;
- deployed deep-link and cache-header smoke.

Exit: local implementation passes. Parent tasks remain `BLOCKED` until separate
Appwrite/Vercel environments and telemetry provide operational evidence.

### Stage 3 — isolation, roles, and routes (`TASK-OWN-001`, `TASK-AUTH-001`, `TASK-PROJ-001`)

Create:

- Workspace/Project ownership records and trusted scope derivation;
- Owner and assigned-Maintainer mapping with deny-by-default policy;
- two-Workspace/two-Project deterministic fixtures;
- global current/historical slug reservation and non-reassignment;
- root, current, history, inactive, reserved, and unavailable route behavior;
- FR/EN root with no Project/Workspace catalog operation.

Validate:

- complete protected-operation isolation matrix;
- Owner/assigned/unassigned/removed/forged role matrix;
- slug collision, history, redirect, direct-link, inactive-intake, and
  root-no-enumeration matrix;
- component and browser checks in FR/EN, by keyboard, and at 320 px.

Exit: local matrices pass, then the same matrices pass against non-production
Appwrite with two real Workspaces and Projects.

### Stage 4 — provider and upload foundations (`TASK-SRC-001`, `TASK-ATT-001`)

Create:

- provider-neutral source-connection ports;
- owner-only connect, callback state/nonce, encrypted grant, repository
  selection, suspend/disconnect, and revocation policy;
- GitHub and GitLab adapters behind those ports;
- reproducible 10 MB plus multipart/protocol-overhead ingress probe.

Validate:

- CSRF, replay, cross-scope, unselected-repository, secret-leak, and revocation
  tests;
- real non-production GitHub App and GitLab OAuth callback exercises;
- deployed Appwrite Function upload probe.

Exit: `TASK-SRC-001` and `TASK-ATT-001` are `DONE`, then Gate G1 passes.

### Stage 5 — Reporter and feedback contracts (`TASK-REP-001`, `TASK-FDB-001`)

Create:

- unidentified, contact, issuer-scoped external-ID, and trusted-assertion
  Reporter attribution with audit history;
- separate Bug, Suggestion, and Review source contracts;
- bounded declarative Context with source, purpose, and trust state;
- FR/EN type selection, form semantics, and intentional Context review.

Validate:

- cross-issuer/application/Workspace identities never merge;
- public identity stays unverified and no fingerprint is collected;
- required, optional, malformed, undeclared, executable, and size-boundary
  source/Context cases;
- source and Context remain separate from derived classification;
- FR/EN, keyboard, language-preservation, non-color, and 320 px checks.

Exit: `TASK-REP-001` and `TASK-FDB-001` are `DONE`.

### Stage 6 — accountless access and acceptance (`TASK-ACC-001`, `TASK-INTAKE-001`)

Create:

- stable human reference and independent high-entropy Feedback proof;
- one-way verifier, rotation/revocation, and Reporter-safe projection;
- `clientOperationId` idempotency and payload conflict detection;
- Appwrite transaction for Reporter, source, Context, `received` history,
  verifier, notification, and durable outbox;
- confirmation and proof-preservation experience.

Validate:

- reference-only, invalid, revoked, and sibling proof return indistinguishable
  denial;
- response-loss retry creates exactly one effect;
- changed-payload key reuse conflicts;
- forced transaction failure returns no success/reference/proof;
- notification failure preserves the accepted source fact;
- Reporter projection contains no Internal Note, internal classification,
  unrelated Feedback, or audit data.

Exit: `TASK-ACC-001` and `TASK-INTAKE-001` are `DONE`.

### Stage 7 — private Attachment saga (`TASK-ATT-002`, `TASK-ATT-003`)

Create:

- private request-owned staging and manifest limits;
- actual-content detection, parsing/decoding, strict UTF-8, malware port,
  digest, archive/executable/polyglot rejection;
- atomic metadata association after all files validate;
- failure cleanup, stale-stage sweeper, and reconciliation;
- scope/audience-authorized download and Feedback lifecycle binding.

Validate:

- seven accepted-format fixtures;
- spoofed, polyglot, malware, archive, executable, binary-text, oversized, and
  sixth-file fixtures;
- any-file and commit failures produce no Feedback success or durable orphan;
- Reporter/internal-evidence, sibling, removed-assignment, and cross-scope
  downloads fail;
- deletion immediately hides files.

Local exit: every locally executable contract and regression passes.
`TASK-ATT-002`, `TASK-ATT-003`, and Gate G2 remain `BLOCKED` until the same
behavior passes through real non-production Appwrite ingress, private Storage,
TablesDB, antivirus, authorization, lifecycle, and cleanup adapters; the Goal
cannot be completed before that evidence exists.

## External evidence lane

Local work continues while these inputs are unavailable, but affected parent
tasks and gates remain `BLOCKED`:

1. separate non-production and production Appwrite projects;
2. linked Vercel project and `feedback.y7labs.studio` control;
3. operational telemetry destination;
4. two distinct production principals;
5. publisher-owned test GitHub App installation;
6. test GitLab.com OAuth application.

### Available non-production input — 2026-08-12

- Appwrite Cloud preview project `Y7 Feedback Preview` exists in Frankfurt.
- Public project ID: `6a7be5dc003be4642e6e`.
- Public API endpoint: `https://fra.cloud.appwrite.io/v1`.
- A Web platform for `localhost` exists.
- A local ignored least-privilege API key became available on 2026-08-23. The
  first successful application created 16 private TablesDB tables, one private
  encrypted antivirus bucket, and seven deterministic G1 fixture rows.
- Replay detected that Appwrite created requested encrypted `varchar`/`text`
  columns with `encrypt: false`. The provisioner correctly reports
  `APPWRITE_INFRASTRUCTURE_DRIFT:table:reporters`; idempotence and G1 therefore
  remain unproved. Appwrite documents database-column encryption as a Pro-plan
  feature, so the project capability must be resolved before safe recreation.

## Execution status

`BLOCKED — APPWRITE COLUMN ENCRYPTION CAPABILITY AND EXTERNAL G1/G2 EVIDENCE REQUIRED`

The Goal plan and mandatory `AGENTS.md` instructions were approved on
2026-08-10. Personal GitHub SSH authentication and origin ownership were
verified. The Aïobi Messenger repository established the intended authorship
convention: the configured human is the commit author, no machine co-author is
added, and cryptographic signing is not required.

Stage 1 was completed with BDD/TDD evidence in commits `f65e3f2` and `89568c7`:

- `pnpm install --frozen-lockfile`, `pnpm format:check`, `pnpm lint`,
  `pnpm typecheck`, `pnpm test`, `pnpm test:coverage`, `pnpm build`,
  `pnpm test:e2e`, and `pnpm security:scan` pass;
- 10 local structural/unit/component/contract tests and 6 browser scenarios
  pass;
- measured TypeScript source coverage is 100% for statements, branches,
  functions, and lines across the domain, Function, and web packages;
- desktop and 320 px Chrome runs pass FR/EN, keyboard, WCAG AA axe,
  horizontal-overflow, manifest, and safe service-worker assertions;
- the built client scan checks 17 source/build/configuration files with zero
  prohibited-data finding;
- visual inspection confirms the Organic sand/sage/clay root direction at
  desktop and 320 px.

Stage 2 local controls were completed in commits `1598aa6` and `8cc7540`:

- public and trusted configuration parsers reject missing, secret-bearing,
  insecure, or mismatched environment input and shared cross-environment
  Appwrite authority;
- trusted Function responses and allowlisted structured events share one opaque
  correlation ID, while prohibited bodies and credential-shaped fields are
  discarded;
- the replaceable browser RUM adapter projects only LCP, INP, and CLS facts;
- the synthetic root/health probe returns only bounded pass/fail facts, and a
  Vercel policy contract verifies immutable assets, revalidated shell files,
  and absence of an Appwrite proxy destination;
- 31 local tests and 6 browser scenarios pass, measured TypeScript coverage is
  100%, and the expanded source/build scan checks 26 files with zero finding.

`TASK-OBS-001` is `IN PROGRESS`, not done, because real measurement series and
the telemetry destination are unavailable. `TASK-DEL-002` and `TASK-VER-001`
also remain blocked pending deployed Appwrite/Vercel cross-environment,
deep-link, domain, and cache-header evidence. Stage 3 can continue with local
ownership, authorization, and route matrices without inventing that evidence.

The Stage 3 domain matrix was completed in commits `0254418` and `c5eefeb`:

- Workspace/Project ownership is immutable and cross-Workspace association is
  rejected with one non-disclosing policy outcome;
- fixed Owner, assigned Maintainer, unassigned/removed Maintainer, sibling and
  Platform Operator decisions cover read, write, search, aggregate, file,
  notification, Realtime, and Project-management capabilities;
- current and historical slugs are globally and permanently reserved, system
  routes cannot be claimed, redirects retain Project identity, and unknown or
  inactive intake produces one neutral unavailable result;
- 39 local tests and 6 browser scenarios pass, measured TypeScript coverage is
  100%, and 29 source/build/configuration files scan with zero finding.

`TASK-OWN-001` and `TASK-PROJ-001` remain `IN PROGRESS`; `TASK-AUTH-001` remains
`BLOCKED`. Completion requires the same matrices at trusted Function,
TablesDB/Storage permission, real Auth principal, Realtime, and deployed route
boundaries. Locally independent Day 2 contracts may proceed while those inputs
remain unavailable.

The locally executable Stage 4 foundation was completed in commits `b4d969c`,
`cf3e92d`, and `35f9fe5`:

- Source Connection policy is provider-neutral and binds initiation/completion
  to an Owner, authoritative Workspace/Project, provider, nonce digest, expiry,
  safe return path, and one-use callback state;
- wrong actor/scope/provider/nonce, expiry, replay, unsafe return, unauthorized
  or duplicate selection, and provider mismatch are denied;
- only explicitly selected authorized repository identities are usable, and
  suspension/disconnection stop use immediately while grant revocation is
  invoked once;
- a deterministic probe constructs exactly 10 MiB of file bytes plus stable
  multipart overhead and reduces success, rejection, or timeout to bounded
  facts without response-body or authorization disclosure;
- 47 local tests and 6 browser scenarios pass, measured TypeScript coverage is
  100%, and 30 source/build/configuration files scan with zero finding.

`TASK-SRC-001`, `TASK-ATT-001`, and Gate G1 remain `BLOCKED`: real GitHub App
and GitLab.com OAuth grants, deployed callbacks/revocation, and an Appwrite
Function ingress execution are still absent. Concrete provider adapters and an
encrypted private grant vault now exist locally, but do not substitute for
those provider exercises. The probe is ready to run as `pnpm probe:ingress`
when the inputs exist.

Stage 5 was completed in commits `a5fad4e`, `1850c4a`, and `76a3304`:

- Reporter attribution creates isolated unidentified and public-identity
  records, retains purpose/provenance and unverified trust, and matches a
  verified assertion only on exact Workspace, issuer, application, and value;
- controlled correction, link, merge, and anonymization append scoped history,
  while malformed or cross-Workspace changes fail;
- Bug, Suggestion, and Review retain distinct source semantics; Reporter,
  original source, declared Context, Attachment manifest, and derived
  classification remain structurally separate;
- bounded Context retains value type, disclosed purpose, source, and trust, and
  rejects duplicate, undeclared, malformed, oversized, non-finite, or
  executable input;
- at the Stage 5 checkpoint, direct `/wisemoney` intake preserved safe input
  through FR/EN changes and provided an intentional local review; later closure
  work connected that review to the trusted intake HTTP boundary;
- the final repository gate passes 70 local tests and 10 browser scenarios;
  domain/config/Function coverage is 100%, web coverage is 98.98% statements,
  96.36% branches, and 100% functions/lines, and 34 scanned files return zero
  finding;
- keyboard, axe WCAG AA, horizontal-overflow, desktop, and 320 px checks pass;
  visual inspection confirms the three-band source/identity/context review.

`TASK-REP-001` and `TASK-FDB-001` are `DONE`. Continuous Day 2
`TASK-SEC-001` and `TASK-UX-001` are `IN PROGRESS`. At that checkpoint, Stage 6
proof and durable acceptance behavior had not started, and Gate G1 remained
explicitly blocked.

The locally executable Stage 6 foundation was completed in commits `090ea8e`,
`40a8042`, `e794643`, `61a76e2`, and `b87e999`:

- a high-entropy Feedback-specific proof stores only a one-way verifier;
  reference-only, missing, invalid, revoked, sibling, unknown, and inconsistent
  grant/record attempts return one non-disclosing denial;
- proof rotation/revocation preserves Feedback identity, reference, ownership,
  and original source, while Reporter clarification, permitted revision, and
  deletion request remain ordered and attributable;
- Reporter projections use an explicit allowlist and exclude Internal Notes,
  workspace classification, non-visible messages/evidence, and sibling data;
- the trusted intake coordinator binds idempotency to Workspace/Project and
  validated payload, and constructs Reporter, Feedback, `received` lifecycle,
  access verifier, notification, outbox, and protected retry result as one
  transaction input;
- exact response-loss retry returns the original reference/proof without a
  second effect, changed payload conflicts, and read, cryptographic envelope,
  or commit failure returns no accepted result, reference, or proof;
- Node cryptography uses a random 256-bit URL-safe proof, SHA-256 verifier,
  timing-safe matching, and AES-256-GCM retry envelopes; tampering, wrong keys,
  malformed material, and invalid nonce/key sizes fail closed;
- `/retrieve` preserves reference/proof through FR/EN changes without URLs or
  persistence, renders only the authorized Reporter view through an injected
  gateway, and truthfully reports retryable unavailability while no deployed
  API adapter exists;
- the repository gate passes 101 local tests and 14 browser scenarios;
  domain/config/Function coverage is 100%, web coverage is 99.31% statements,
  97.43% branches, and 100% functions/lines, and 41 scanned files return zero
  finding.

`TASK-ACC-001` and `TASK-INTAKE-001` remain `BLOCKED`, not `DONE`: concrete
TablesDB transaction, trusted HTTP, project lookup, and accountless repository
adapters pass locally, but no non-production transaction, uniqueness
constraint, immediate retrieval/action, or forced rollback has executed
against Appwrite. Gates G1 and G2 remain blocked.

The locally executable Stage 7 foundation was completed in commits `288485b`,
`cfe7648`, `6516bc6`, `8ed2b92`, and `9afc68e`:

- byte-derived validation structurally accepts JPEG, PNG, WebP, GIF, PDF,
  strict UTF-8 TXT, and CSV while deriving bounded size, SHA-256 digest, safe
  name, and media type independently of client MIME;
- archive/executable signatures, malformed structures, spoofing, polyglots,
  binary or invalid UTF-8 text, executable text, malware, scanner failure, and
  files above exactly 10 MiB fail closed;
- the acceptance saga allows one to five files, stages only operation-owned
  private objects, associates all metadata in one injected transaction, and
  returns no success material after validation, staging, or commit failure;
- cleanup includes every intended operation-owned object; an idempotent sweeper
  removes stale unassociated objects, retains committed objects, contains
  per-object failure, and recovers deferred cleanup;
- Attachment metadata binds one Feedback, Workspace, Project, source entry,
  audience, private object, and lifecycle; Reporter and workspace access use
  exact current authorization, and unknown, sibling, removed, cross-scope,
  soft-deleted, or purged access returns one denial before bytes are read;
- the final repository gate passes 143 local tests and 14 browser scenarios;
  domain, configuration, and Function coverage is 100%, web coverage is 99.31%
  statements, 97.43% branches, and 100% functions/lines; frozen install,
  format, lint, typecheck, build, and a 45-file zero-finding scan pass.

`TASK-ATT-002` and `TASK-ATT-003` remain `BLOCKED`, not `DONE`: concrete private
Storage staging, staging ownership, compensating deletion, metadata
transactions, strict metadata reads, and Node SDK adapters now pass locally.
The workspace still has no real non-production Appwrite Function ingress,
private bucket/tables, antivirus service, authenticated authorization route, or
deployed cleanup/lifecycle evidence. Gates G1/G2 remain externally blocked.

## Adapter and delivery closure — 2026-08-10

The post-stage closure replaced remaining in-memory or unavailable product
boundaries where local implementation was honest:

- `8e3c05a` and `ac7cce9` enforce the complete pull-request quality workflow and
  install the pinned Playwright browser through pnpm;
- `a08c7ac`, `e7b46d4`, `43eccfc`, `9d95206`, `2f79b43`, `3dd6b24`, and
  `5307b24` connect trusted HTTP intake and accountless retrieval to strict
  Appwrite TablesDB project, transaction, grant, and projection adapters;
- `0b9234f` and `fac0c1e` connect the WiseMoney review and retrieval screens to
  trusted HTTP gateways without persisting proof material or placing it in a
  URL;
- `621a0bb`, `5afea09`, and `c199d4b` implement GitLab OAuth, GitHub App, and a
  private provider-bound AES-256-GCM Appwrite grant vault with a separate
  server-only key;
- `07cd781`, `0ef4c21`, and `cf88ed2` implement private Appwrite Storage
  staging, compensating cleanup, strict staging ownership, and atomic
  Attachment metadata persistence/readback.

The latest complete local gate passes 214 tests, including 122 trusted
Function tests, with 100% statement/branch/function/line coverage in the
domain, configuration, and Function packages. Web coverage remains above the
repository threshold, 14 Playwright scenarios pass, and the security scan
checks 58 files with zero findings.

PR #1 was merged into `main` as `c70ea61`. Its first Browser gate exposed and
then received a fix for clean-runner build ordering (`040b7c7`). Later Appwrite
infrastructure and fixture commits are pushed separately on
`task/TASK-DEL-002-appwrite-preview`; that branch has no pull request yet.
Repository-rules configuration and external G1/G2 evidence remain operational
work.

## Appwrite preview branch validation — 2026-08-23

The Appwrite preview branch now includes deterministic infrastructure and G1
fixture provisioning through `a79195b`. The latest commit adds complete
validation and rollback evidence for Node SDK metadata and fixture transaction
failures without reducing the 100% Function coverage threshold. Locally,
format, lint, typecheck, tests, coverage, build, all 14 Playwright scenarios,
and the 64-file zero-finding security scan pass. The commit is pushed and both
the Quality and Browser jobs in GitHub CI run `32655459903` completed
successfully.

## Real Appwrite provisioning attempt — 2026-08-23

With local ignored secrets configured, real preview execution exposed an
Appwrite bulk-create contract mismatch: indexes use `attributes` in the create
request but `columns` in table metadata. Commit `4bd3930` corrects that boundary
translation with a contract test; all local quality gates, 100% Function
coverage, and 14 Playwright scenarios pass.

The corrected first application returned infrastructure `created: 17`,
`verified: 1` and fixtures `created: 7`, `verified: 0`: the existing database
was verified, 16 tables plus the bucket were created, and two Workspace/two
Project slug fixtures were seeded. The mandatory replay returned
`APPWRITE_INFRASTRUCTURE_DRIFT:table:reporters` because columns declared with
`encrypt: true` are reported by Appwrite as unencrypted. Representative
`varchar` and `text` metadata confirmed the same result. No secret or row
content was read or recorded.

The remote schema is non-production but nonconforming and is not accepted as
G1 evidence. Existing columns cannot be upgraded to encrypted through the
TablesDB update API. Before destructive cleanup, the project must support
encrypted database columns and the operator must explicitly approve deleting
and recreating the generated preview tables/fixtures. Until then, the drift is
preserved as a fail-closed blocker.

## Application-envelope resolution — 2026-08-23

The operator accepted the recommended resolution: retain the Appwrite Education
project and protect sensitive values in trusted Functions. ADR-004 records the
AES-256-GCM envelope, contextual associated data, versioned keyring, purpose
separation, rotation, and fail-closed adapter requirements.

Commit `70bdd48` implements and tests the envelope primitive. The current task
branch additionally integrates it into intake, accountless access, and
Attachment metadata persistence; makes the production composition root require
the configured keyring; and changes the manifest to explicitly match the
Education project's unencrypted envelope columns. Queryable scope and state
remain plaintext, while protected payloads remain unindexed.

Local implementation is not yet real-service evidence. The next gate is to add
an independent sensitive-data keyring to the ignored preview environment, pass
the complete quality suite, commit and push the adapter slice, then obtain
explicit authorization before deleting and recreating the nonconforming remote
tables and fixtures. G1 remains `BLOCKED` until the recreated provision and its
mandatory replay both pass.

## Appwrite Education reprovision and replay — 2026-08-23

After explicit operator authorization, exactly the 16 manifest-owned preview
tables were deleted. The database and private Attachment bucket were preserved.
The clean provision returned infrastructure `created: 16`, `verified: 2` and
fixtures `created: 7`, `verified: 0`.

Real replay exposed two representation-only differences: Appwrite omits the
default `encrypt: false` property from normalized column definitions and returns
UTC datetime values using `+00:00` instead of `Z`. The manifest now omits the
native-encryption default, and the fixture boundary canonically normalizes the
equivalent UTC suffix. Both corrections have automated coverage.

The mandatory final replay passed with infrastructure `created: 0`,
`verified: 18` and fixtures `created: 0`, `verified: 7`. This closes the
Appwrite infrastructure/fixture drift blocker without treating native database
encryption as available. Sensitive application fields remain protected by the
ADR-004 contextual envelopes. Remaining G1/G2 actor, isolation, and deployed
service evidence is still required before the broader gate can be marked done.

## Real intake and accountless matrix — 2026-08-24

Draft PR `#2` now tracks the branch and GitHub Actions reports both `Quality
gate` and `Browser gate` successful. The PR remains draft while required G1/G2
external evidence is incomplete.

The `pnpm verify:appwrite:g1` command builds emitted Node workspaces, loads
only the ignored preview environment, and exercises the trusted public API
against real non-production TablesDB. The matrix proves initial acceptance,
exact idempotent replay with the same proof, payload-hash conflict, the same
non-disclosing denial for an invalid proof, authorized accountless retrieval,
rotation of the Access Proof, denial of the old proof, authorization with the
rotated proof, revocation, denial of the revoked proof, and application
envelopes rather than sensitive plaintext in persisted rows. Cleanup deleted
all seven uniquely named synthetic rows.

The first execution exposed two real boundary defects that local fakes had
missed: the domain package exported TypeScript source to Node runtime, and the
intake adapter sent undeclared `id` attributes inside lifecycle and notification
documents. The workspace export now separates source types from emitted ESM,
and TablesDB payloads use `rowId` as identity without duplicate attributes.
Automated regressions cover runtime resolution, every fail-closed matrix stage,
cleanup failures, and exact payload shape. The final real result was:

```text
accepted=true replayed=true conflictDenied=true invalidProofDenied=true
authorizedRetrieval=true rotated=true oldProofDenied=true
rotatedProofAuthorized=true revoked=true revokedProofDenied=true
sensitiveRowsEncrypted=true cleanedRows=7
```

The same command now injects a deterministic failure before the third row of a
real TablesDB transaction. The API returns a retryable result, all seven target
rows are verified absent, and the defensive cleanup finds zero visible rows.
This proves the real rollback boundary rather than only the in-memory adapter.

The durable outbox slice claims rows transactionally, stores attempts, retry
time, leases, and terminal facts inside the contextual encrypted payload, and
uses the Notification ID as the provider idempotency key. A verification-only
candidate guard prevents the matrix from claiming unrelated rows. Two isolated
real intakes prove retry scheduling, recovery to delivery, permanent failure,
an idle deduplication pass, encrypted terminal rows, and cleanup of all fourteen
synthetic rows. The sender is deterministic in this matrix, so this is real
TablesDB/outbox evidence but not an assertion that an external email was sent.

`TASK-ACC-001` and `TASK-INTAKE-001` remain `BLOCKED`, rather than being
overstated as done: deployed accountless/intake ingress and real notification
delivery evidence are still required.

GitHub CI for `1ac92d1` then exposed a clean-runner-only ordering defect: the
`Test` step resolved the correct emitted ESM exports before `config` and
`domain` had been built. The root `test` and `test:coverage` commands now build
those two runtime workspaces first. `BDD-CI-006` locks this ordering, and a local
validation starting with both ignored `dist` directories absent passes.

## Appwrite Function deployment — 2026-08-25

The Preview API now has a source-controlled `appwrite.config.json` with one
publicly executable Function, a dynamic key restricted to `rows.read` and
`rows.write`, no event or schedule trigger, and no committed variables. The
runtime composition derives the endpoint, Project, and per-execution key from
Appwrite's injected Function authority and fails closed when that authority is
missing, duplicated, or contradicts local configuration.

Appwrite Cloud's real Frankfurt runtime catalog does not offer Node 24 for this
project. The manifest therefore uses its available Node 22 LTS runtime while
the workspace accepts Node `>=22 <25` and CI remains pinned to Node 24. The
first Node 24 push failed before creating a Function. The corrected Node 22 push
created and activated deployment `6a8ce80f76c3ccb8e218`; its remote build used
pnpm 10.32.1 and compiled configuration, domain, and API workspaces
successfully.

After explicit operator authorization, the provisioner created exactly 25
secret variables. Its mandatory replay created none and updated all 25 without
duplication. It excludes `APPWRITE_API_KEY`, `APPWRITE_ENDPOINT`, and
`APPWRITE_PROJECT_ID` because the Function receives those values through its
dynamic execution authority. An authenticated SDK execution and a separate
public execution without any API key both completed `GET /health` with HTTP 200
and `{ "status": "ok" }`, proving the deployed runtime and public execute rule.

The CLI could not create the default Function domain because an Appwrite
Console session is not active, and the deterministic domain returns
`router_rule_not_found`. Direct-domain and ingress-size evidence therefore
remain `BLOCKED`; the public execution evidence below does not substitute for
those HTTP-boundary checks.

## Public deployed G1 execution — 2026-08-25

The reproducible `pnpm verify:appwrite:deployed:g1` command invokes the active
Function through Appwrite's public execution API without an API key. It proves
unknown-Project and invalid-input denial, initial acceptance, exact replay,
changed-payload conflict, indistinguishable reference-only and invalid-proof
denial, authorized retrieval, Reporter-safe projection, and encrypted persisted
payloads. No reference, Access Proof, marker, row value, or execution key is
printed.

The first matrix run exposed a verifier-only cleanup defect: Feedback
intentionally has no `reference` column, so cleanup could not rediscover the
accepted row from that table. A strict two-second preflight found exactly the
seven synthetic rows, validated every cross-row identity, and removed all
seven. The corrected verifier starts from the deterministic Idempotency row,
then follows authoritative Feedback, Reporter, Lifecycle, Grant, Notification,
and Outbox relations. The final run passed every scenario and removed seven
rows; a separate read-only audit of all seven tables returned
`residualRows=0`.

The complete local gate passes with 221 Function tests, 100% Function coverage,
14 Playwright scenarios, and a 78-file security scan with zero findings.

## Direct Function domain and 10 MiB ingress — 2026-08-25

The Appwrite-managed domain
`https://y7-feedback-api-preview.appwrite.network` serves the active Preview
Function and returns HTTP 200 for `GET /health`. A Preview-only operational
route verifies the binary body received by the runtime against deterministic
file and total-byte headers, remains unavailable in Production, rejects missing
or inconsistent evidence, and never parses multipart content as JSON.

The first real probe exposed two boundary defects rather than an Appwrite size
limit: domain methods require normalization at the transport boundary, and the
runtime's lazy `bodyJson` getter throws when accessed for multipart data. After
BDD/TDD correction, a second probe exposed that both deployed environment
labels were still `development`. The ignored Preview environment was corrected
to `preview`, all 25 secret Function variables were resynchronized without
creating duplicates, and a new deployment captured the corrected environment.

The final reproducible `pnpm probe:ingress` result was:

```text
accepted=true fileBytes=10485760 status=200 totalBytes=10485931
```

This closes the required deployed Appwrite ingress-size evidence. PR #2 merged
commit `25a50cc` into `main` as `45ee705`, satisfying the final task gate and
moving `TASK-ATT-001` to `DONE`. The ingress result alone does not claim
external antivirus, provider callback, or external notification delivery
evidence.

## Direct-domain deployed G1 execution — 2026-08-25

The deployed G1 verifier now selects either Appwrite's public execution API or
the managed Function domain without changing its scenario matrix. The direct
HTTP adapter accepts only a root HTTPS origin, allow-listed methods, safe
relative paths, bounded requests, JSON object responses, and stable
non-disclosing transport failures. Its contract has 16 BDD examples covering
successful forwarding, empty requests, unsafe origins/methods/paths, malformed
responses, and network failure.

The real `pnpm verify:appwrite:deployed:g1:domain` run against the Preview
Function proved unavailable-Project and invalid-input denial, initial intake,
exact replay, changed-payload conflict, indistinguishable reference-only and
invalid-proof denial, authorized retrieval, proof rotation, old-proof denial,
rotated-proof retrieval, revocation, revoked-proof denial, encrypted sensitive
rows, and cleanup of exactly seven synthetic rows. The run printed only bounded
facts:

```text
transport=direct-domain accepted=true replayed=true conflictDenied=true
referenceOnlyDenied=true invalidProofDenied=true authorizedRetrieval=true
rotated=true oldProofDenied=true rotatedProofAuthorized=true revoked=true
revokedProofDenied=true
sensitiveRowsEncrypted=true cleanedRows=7
```

This closes the deployed access-proof behavior matrix. `TASK-ACC-001` moves to
`IN PROGRESS` until the atomic change is merged and the final task gate is
recorded. Real external notification delivery remains missing, so
`TASK-INTAKE-001` remains `BLOCKED`.

## Public Vercel Preview evidence — 2026-08-25

The Vercel project `y7-feedback` is linked locally and deployment protection was
explicitly disabled for this Preview project so unidentified Reporter journeys
can reach it. Deployment `dpl_ARivdqCDfF3Dbke4TjMrYzbjtz8Q` is `READY` at:

```text
https://y7-feedback-lb5nynkjj-y4nn-projects.vercel.app
```

Outside-in BDD/TDD added a bounded public deployment probe. It fails closed on
Vercel authentication, non-success responses, a changed SPA shell, unsafe
cache policy, missing security headers, or a missing hashed asset. It tolerates
only Vercel's known terminal feedback-toolbar script when comparing the root
and deep-link documents. The SPA fallback also excludes `/api/*`, preventing a
reserved API path from being represented as a successful application shell.

The real probe passed with these bounded results:

```text
status=ok public=true deepLink=true shellRevalidated=true
pwaRevalidated=true immutableAsset=true securityHeaders=true
reservedApiDenied=true
```

The deployment contains only the public Preview authority and release label;
the repository security scan checked 79 files with zero findings. Fourteen
Playwright scenarios also pass. At the time of this first manual deployment,
automatic task-branch creation was still unproved; the later Git-linked
evidence below supersedes that limitation. The controlled product domain and
Preview/Production isolation gate remain unavailable, so `TASK-DEL-002`
remains `BLOCKED`.

## Deployed Reporter Attachment authorization — 2026-08-25

Outside-in BDD/TDD added a capability-focused Reporter Attachment coordinator.
It derives the authoritative Feedback identity from the reference and Access
Proof before metadata or Storage access, maps missing and sibling files to one
non-disclosing denial, and returns dependency failure separately. The public
Function route emits real binary bytes with `no-store`, an RFC 5987 filename,
an allow-listed actual media type, and a bounded content length. The direct
Function-domain adapter rejects malformed, cacheable, oversized, mismatched,
or unsafe binary responses.

The Function manifest grants only the additional `files.read` scope. Deployment
`6a8d0b1fab8968184d2c` exposed that Appwrite's binary helper requires an
explicit Node `Buffer`; the initial deployed byte comparison failed while
status, filename, and media type passed. The Buffer correction was activated in
deployment `6a8d0be6831b77d0ce12`.

The final real matrix creates two independent Feedback grants, downloads the
first Attachment with its correct proof, denies the same Attachment using the
second valid proof, and removes every synthetic artifact. Its bounded result
was:

```text
accepted=true privateFile=true metadataEncrypted=true
deployedAuthorizedDownload=true deployedSiblingDenied=true
deployedSiblingCleanedRows=7 removedObject=true cleanedRows=8
sweeper.orphanRemoved=true sweeper.associatedRetained=true
sweeper.cleanedRows=1
```

The API package passes 269 tests with 100% statement, branch, function, and line
coverage; all 14 Playwright scenarios and the 80-file zero-finding security
scan pass. `TASK-ATT-003` moves to `IN PROGRESS`, not `DONE`: deployed
soft-delete/restore/purge behavior and real workspace actor assignment/removal
authorization are still missing.

## Real Attachment lifecycle evidence — 2026-08-25

Outside-in BDD/TDD added an exact Workspace/Project lifecycle policy, a trusted
coordinator, and a capability-focused Appwrite compare-and-set store. Each
transition reads and updates the Attachment lifecycle in one Appwrite
transaction; stale state, malformed metadata, write failure, and rollback
failure return one stable retryable outcome without leaking infrastructure
details. Purge changes authoritative state before removing the private object,
so a failed removal remains retryable and cannot restore Reporter visibility.

The real Preview matrix used this trusted coordinator against Appwrite and
observed every visibility change through the deployed public Function domain.
It proved immediate denial after soft-delete, exact-byte download after restore,
immediate denial after purge, idempotent purge retry, physical Storage removal,
and deterministic cleanup. Its bounded result included:

```text
deployedSoftDeleteHidden=true deployedRestoreAuthorized=true
deployedPurgeHidden=true deployedPurgeRemoved=true
removedObject=true cleanedRows=8
```

Domain coverage passes 47 tests and API coverage passes 284 tests, both at
100% statements, branches, functions, and lines. The complete local gate passes
with 14 Playwright scenarios and an 82-file security scan with zero findings.
`TASK-ATT-003` remains `IN PROGRESS`, not `DONE`: a real Auth-backed Workspace
actor assignment/removal matrix is still required. Lifecycle evidence does not
substitute for that authorization evidence.

## Real Appwrite Auth and Workspace Attachment evidence — 2026-08-25

The security-sensitive design selected Appwrite JWT verification rather than
trusting transport headers or creating a second application session system.
The Function binds each bearer JWT to a fresh `Account.get`, then uses its
server capability to reload the private Project, Workspace membership, and
Project assignment before every protected download. Client-supplied principal,
Workspace, and Project values cannot replace route-derived scope or the
Appwrite principal. Authorization failures share one non-disclosing response.

BDD/TDD covers malformed and expired identity, forged principal mismatch,
malformed authoritative rows, duplicate scope rows, removed membership,
removed assignment, sibling Project, cross-Workspace, inactive Project,
dependency failure, and the successful Owner/Maintainer paths. Deployment
`6a8d14c47f2dc24f0e80` exposed the route on the Preview Function domain.

The real matrix created two temporary Appwrite Auth Users and a private
Internal Note Attachment. It proved Owner access, denial before Maintainer
assignment, access after assignment, body-principal forgery denial,
cross-scope denial, immediate denial after assignment removal, immediate denial
after membership removal, and denial after session revocation. It then reread
all Auth Users, role rows, Attachment metadata, staging metadata, and Storage
file to prove their absence. The bounded result was:

```text
workspaceAuth.ownerAuthorized=true
workspaceAuth.unassignedDenied=true
workspaceAuth.maintainerAuthorized=true
workspaceAuth.forgedDenied=true
workspaceAuth.crossScopeDenied=true
workspaceAuth.assignmentRemovalDenied=true
workspaceAuth.membershipRemovalDenied=true
workspaceAuth.sessionRevocationDenied=true
workspaceAuth.cleaned=true
```

`TASK-AUTH-001` moves from `BLOCKED` to `IN PROGRESS`; `TASK-ATT-003` remains
`IN PROGRESS`. Their real task-specific matrices now pass, but neither becomes
`DONE` before this atomic tranche is merged and the final task gate is recorded.

## Real direct Appwrite isolation evidence — 2026-08-25

Outside-in BDD/TDD adds a fail-closed matrix for two temporary Appwrite Auth
principals representing Owner and Maintainer. Against Preview, each principal
observed zero rows/files and could not read a known private sentinel across
Projects, Feedback, Notifications, Source Connections, Provider Grants, and
Attachment Storage. Direct create, update, and delete attempts were also
denied.

Realtime isolation uses a third temporary Auth principal as a positive control.
The control alone receives an update to its permission-scoped Provider Grant;
the Owner and Maintainer subscriptions to the exact same channel receive no
event. This prevents a broken WebSocket from being accepted as isolation. The
bounded successful result was:

```text
status=ok environment=preview principalsChecked=2 surfacesChecked=6
listIsolation=true sentinelIsolation=true mutationIsolation=true
realtimeIsolation=true cleaned=true
```

An initial Realtime attempt exposed the browser SDK's `window` heartbeat
assumption under Node 24 and terminated before its asynchronous cleanup. A
recovery pass first proved one complete fixture set by its shared random suffix,
exact IDs, names, and payload markers, then removed only those 3 principals, 9
rows, and 1 file. A final read-only audit returned zero rows on all four fixture
tables, zero files, and zero principals.

Vercel Authentication was explicitly disabled through the project API and the
current routed Preview URL returned anonymous HTTP 200. This proves public
reachability for Reporter journeys. The subsequent Git-linked deployment below
also proves automatic creation from the pushed task branch; Preview/Production
cross-environment denial remains unavailable.

`TASK-OWN-001` remains `IN PROGRESS`: direct Appwrite bypass is now disproved,
while the complete cross-scope matrix for every protected Function operation
and merge evidence remain required. `TASK-VER-001` and `TASK-DEL-002` retain
their unresolved controlled-domain and environment-separation gates.

## Automatic Vercel task-branch Preview evidence — 2026-08-25

Pushing `56cf05d` proved that the Vercel GitHub connection now observes
`task/TASK-ACC-001-direct-domain`, but its clean-clone build failed because the
repository build command compiled Config and Web without first compiling the
Web runtime dependency `@y7-feedback/domain`. The local build had concealed
this because Domain output already existed.

`BDD-VER-002` was changed red-first to require every compiled Web dependency.
Commit `3a5ebbc` adds the Domain build before Web. Its push automatically
created a `READY` Preview from the exact Git branch and commit at:

```text
https://y7-feedback-jpgpgqvm7-y4nn-projects.vercel.app
```

The deployed probe returned:

```text
status=ok public=true deepLink=true shellRevalidated=true
pwaRevalidated=true immutableAsset=true securityHeaders=true
reservedApiDenied=true
```

GitHub Actions run `32831796557` completed both Quality and Browser gates
successfully. This closes automatic task-branch Preview linkage and anonymous
Reporter reachability. `TASK-VER-001` remains `IN PROGRESS` only for the
controlled product domain and final Preview/Production isolation evidence;
`TASK-DEL-002` remains `BLOCKED` on the separate Production environment.

## Consolidation Draft PR evidence — 2026-08-25

Draft PR [#3](https://github.com/Y4NN777/y7-feedback-mngt-system/pull/3)
consolidates the 13 atomic commits on
`task/TASK-ACC-001-direct-domain` into `main`. GitHub reports the branch as
mergeable. The description traces `TASK-ACC-001`, `TASK-ATT-003`,
`TASK-AUTH-001`, `TASK-PROJ-001`, `TASK-OWN-001`, and `TASK-VER-001`, and keeps
the missing Production, controlled-domain, provider-callback, notification,
and antivirus evidence explicitly blocked.

Pull-request run `32837082485` completed its Quality and Browser gates
successfully; the Vercel Preview Comments integration also completed
successfully. No task moves to `DONE` before PR #3 is reviewed and merged.

## Consolidation merge evidence — 2026-08-25

PR [#3](https://github.com/Y4NN777/y7-feedback-mngt-system/pull/3) left
Draft after explicit operator authorization, retained six successful checks and
no base conflict, then merged 14 atomic commits into `main` using GitHub's
linear `Rebase and merge` method. GitHub records merged commit `bb4472d` and
deleted the remote task branch. Local `main` fast-forwarded from `45ee705` to
`bb4472d` without local changes or history rewriting.

The merge closes the remaining task gates for `TASK-ACC-001`,
`TASK-AUTH-001`, and `TASK-ATT-003`, whose complete task-specific real matrices
and cleanup evidence were already recorded above. It does not close
`TASK-OWN-001`, `TASK-PROJ-001`, `TASK-VER-001`, `TASK-DEL-002`, or any task
still awaiting Production, controlled-domain, provider, notification,
telemetry, or antivirus evidence.

## Next executable slice — `TASK-OWN-001` trusted Function isolation

The remaining ownership gap is not direct Appwrite access, which already
fails, but cross-scope behavior through trusted Function operations. The slice
uses capability-focused production coordinators behind one authoritative
Appwrite principal/scope resolver. It will not add a generic action dispatcher
or a verification-only route that could create a second authorization model.

### Behavior matrix

| Scenario | Given | When | Then |
| --- | --- | --- | --- |
| BDD-OWN-FUNCTION-001 exact-scope Owner | Two real Workspaces and Projects, an Owner of Workspace A, and fixtures in both scopes | The Owner performs Feedback create/read/update/delete, search, aggregate, Attachment read, Notification read, and Realtime authorization for Project A | Every operation succeeds only for authoritative Workspace A / Project A data and returns no foreign identifier or count. |
| BDD-OWN-FUNCTION-002 cross-Workspace Owner denial | The same fixtures and Owner | The Owner targets Workspace B or Project B through every protected operation | Every operation returns the operation's stable non-disclosing denial, performs no mutation, returns no row/file/count/event, and does not reveal existence. |
| BDD-OWN-FUNCTION-003 assigned Maintainer scope | A Maintainer assigned only to Project A | The Maintainer performs the complete protected-operation matrix | Allowed capabilities succeed only in Project A; Project management and every Project B operation fail closed. |
| BDD-OWN-FUNCTION-004 removed assignment | The Maintainer's assignment is removed after one successful control operation | The same JWT retries every protected operation | Fresh authoritative scope derivation denies all operations immediately, including file, notification, aggregate, and Realtime authorization. |
| BDD-OWN-FUNCTION-005 forged and mismatched scope | A valid principal JWT plus client-supplied principal, Workspace, Project, row, file, filter, or channel identifiers from another scope | Any protected Function operation is requested | Client claims never replace authoritative records; the request is denied before protected data access or mutation. |
| BDD-OWN-FUNCTION-006 dependency and rollback failure | A valid exact-scope actor and a forced read, write, or transaction failure | A protected operation executes | It returns a stable retryable outcome, never a false denial or success, and leaves no partial cross-scope or durable mutation. |
| BDD-OWN-FUNCTION-007 real zero-residue matrix | The complete Preview matrix has finished | The verifier rereads fixture principals, rows, files, subscriptions, and mutation targets | All uniquely owned synthetic artifacts are absent and unrelated data was never selected for cleanup. |

Red starts with contract tests proving the missing operation surface and
authorization order. Green adds the smallest production coordinator/store
capabilities required by the matrix. Refactor keeps scope resolution shared but
ports segregated by read, write, aggregate, notification, file, and Realtime
capability. Completion requires the same matrix against real Appwrite Preview,
then CI and merge evidence.
