# Y7 Feedback — actionable plan for Days 3–5

## Purpose

Complete the remaining scope of sprint `S-01` after the verified Day 1 and
Day 2 gates. This plan preserves the normative requirements and does not start
implementation. A task becomes `DONE` only after its positive, negative,
isolation, retry, failure, deployed, accessibility, and cleanup evidence pass.

The original calendar dates are historical. `Day 3`, `Day 4`, and `Day 5` are
execution phases and gates, not claims that the remaining work fits in three
literal days.

## Current baseline

- Day 1 and Day 2 ledgers are `DONE`; G1 and G2 have real Preview evidence.
- Appwrite Preview and Production, Vercel Preview and Production, GitHub, and
  GitLab authorities exist and are isolated.
- The provider OAuth foundation, repository discovery, explicit selection,
  revocation, and replay denial already pass against real services.
- Durable intake/outbox behavior and a Preview mail catcher already pass.
- Notification rows, scoped notification reads, and Realtime authorization
  ports exist, but the complete product notification experience does not.
- The PWA shell, service worker, safe-cache baseline, FR/EN public experience,
  and deployed telemetry exist.
- Conversation, lifecycle, administration UI, maintainer workbench, issue
  linking, provider synchronization, offline outbox, Intelligence, privacy,
  backup/recovery, anti-abuse, and Platform Operator workflows are not yet
  implemented as complete product slices.
- Permanent Production antivirus deployment is a release-readiness action. It
  must exist before Production uploads are enabled, but it does not reopen G2.

## Honest initial task states

| Task | Initial state | Existing foundation | Missing completion evidence |
| --- | --- | --- | --- |
| `TASK-ADMIN-001` | `TODO` | Trusted Workspace/Project scope and roles | First-party administration domain, API, UI, invalid-operation matrix |
| `TASK-SRC-002` | `IN_PROGRESS` | Real OAuth, discovery, selection, revocation | FR/EN selection UI, release import, health/disconnect UI, badge |
| `TASK-CONV-001` | `TODO` | Feedback and authorization primitives | Separate Message/Internal Note models, append API, projections, UI |
| `TASK-LIFE-001` | `TODO` | Feedback lifecycle identifiers | Full transition machine, attribution, reopening and invalid-transition proof |
| `TASK-WORK-001` | `TODO` | Scoped reads/search/aggregate ports | Maintainer inbox, detail, dashboard, actions, classification and deletion |
| `TASK-NOT-001` | `IN_PROGRESS` | Notification rows, scoped list, Realtime authorization | Recipient rules, feed UI, invalidation/refetch and latency evidence |
| `TASK-NOT-002` | `IN_PROGRESS` | Durable outbox and Preview SMTP proof | Day 3 templates/events, reconciliation, provider handoff timing |
| `TASK-ISSUE-001` | `TODO` | Selected provider repositories | Consent versioning, payload policy, visibility check, one-link invariant |
| `TASK-PWA-001` | `IN_PROGRESS` | Installable shell and cache safety baseline | Full offline navigation, update lifecycle and cache inventory proof |
| `TASK-OFF-001` | `TODO` | Idempotent server intake | Partitioned IndexedDB drafts/blobs/projections/outbox and erasure |
| `TASK-OFF-002` | `TODO` | Server idempotency/conflict primitives | Connectivity probe, ordered replay, dependencies, 429 and conflict UI |
| `TASK-INT-001` | `TODO` | Scoped search/aggregate foundation | Complete filters, trends, deleted-data exclusion and UI |
| `TASK-INT-002` | `TODO` | None beyond scoped domain IDs | Themes, relations, provenance, correction/removal |
| `TASK-PRIV-001` | `TODO` | Proof revocation and attachment lifecycle primitives | Request, anonymization, restore, 30-day purge and non-resurrection |
| `TASK-REC-001` | `TODO` | Deployment separation | Encrypted backup, expiry, isolated restore, deletion replay and runbook |
| `TASK-ABUSE-001` | `TODO` | HMAC/crypto primitives | Four expiring counter limits, safe 429 and no raw-IP history |
| `TASK-PLAT-001` | `TODO` | Two-principal authorization evidence | Independent grant/approve/use/revoke/expire and break-glass audit |
| `TASK-SYNC-001` | `TODO` | GitHub/GitLab adapters and grants | Durable inbox/outbox, state mapping, dedupe, loop prevention, reconcile |
| `TASK-SYNC-002` | `TODO` | Conversation and provider adapters pending | Message sync, author verification, revisions/tombstones and consent |
| `TASK-SLO-001` | `IN_PROGRESS` | Correlation, probes, RUM and real analytics series | Load envelope, histograms, alerts and SLO report |
| `TASK-E2E-001` | `TODO` | G1/G2 and public Playwright suites | UC-01..12 and ERR-001..019 named end-to-end evidence |
| `TASK-REL-001` | `TODO` | Preview/Production delivery spine | Production audit, permanent dependencies, smoke and rollback rehearsal |
| `TASK-GHDP-001` | `BLOCKED` | Real GitHub Preview integration | Production use, reachable support contact, release and submission receipt |

## Working method

### TDD/BDD cycle

For every behavior:

1. write the failing domain or contract scenario with its requirement ID;
2. implement the smallest domain behavior;
3. add trusted Function and persistence adapters;
4. prove negative scope, retry, failure, and cleanup paths;
5. add the FR/EN UI and browser scenario;
6. deploy Preview and collect non-sensitive evidence;
7. refactor only while all gates remain green.

SOLID boundaries remain explicit: domain policy does not depend on Appwrite,
React, GitHub, GitLab, SMTP, IndexedDB, or telemetry implementations. Provider
and persistence details implement narrow ports owned by the use case.

### Git and PR strategy

- Use one integration branch per gate: `task/DAY-3-G3`, `task/DAY-4-G4`, and
  `task/DAY-5-G5`.
- Produce atomic commits inside the branch; each commit must leave the branch
  buildable and must include its own tests.
- Use one PR per gate rather than one PR per small task. Split a separate PR
  only for a high-risk migration or provider deployment that needs independent
  rollback.
- Rebase before review and merge by rebase after required checks pass.
- Author all commits as `Y4NN777 <axel.studiesmail@gmail.com>` and sign them
  when the repository rule requires it.
- Never commit `.env.*`, provider tokens, Appwrite keys, Access Proofs, HMAC
  authorities, private identifiers, captured emails, or raw provider payloads.

Atomic commit examples:

```text
feat(admin): enforce project administration policies
feat(conversation): separate messages from internal notes
feat(lifecycle): implement attributable feedback transitions
feat(workbench): add scoped maintainer feedback workspace
feat(notifications): deliver scoped feed and email events
feat(sources): complete repository import and badge flow
feat(issues): enforce consent-aware issue linking
feat(offline): add partitioned durable client outbox
feat(sync): reconcile provider lifecycle and messages
feat(intelligence): add scoped trends and provenance
feat(privacy): implement deletion restore and purge
feat(platform): enforce independent exceptional access
test(release): prove full-system recovery and rollback
```

### Standard local quality gate

Run after every atomic slice and before each gate PR:

```bash
pnpm install --frozen-lockfile
pnpm format:check
pnpm lint
pnpm typecheck
pnpm test
pnpm test:coverage
pnpm build
pnpm test:e2e
pnpm security:scan
```

Every new real-service verifier must be added as a named `pnpm verify:*`
command and to CI where it can run without long-lived secrets.

## Day 3 — feedback loop and administration

### D3.0 — Gate entry

Actions:

- merge the Day 1/2 evidence ledger;
- rerun frozen install, standard local quality gate, deployed G1, deployed G2,
  provider callback, and environment-isolation smokes;
- provision Day 3 schema only after its migration plan and rollback test pass;
- confirm Preview mail-catcher and selected GitHub/GitLab repositories remain
  isolated fixtures.

Exit: the Day 3 branch starts from a clean `main`; no Day 1/2 regression exists.

### D3.1 — `TASK-ADMIN-001`

Build:

- domain commands for Project creation/update, type/Context configuration,
  slug change/history, activation/deactivation, Maintainer assignment/removal;
- trusted Function endpoints deriving Workspace/Project scope from the actor;
- transactional Appwrite adapters and first-party FR/EN administration UI;
- immutable audit facts for every accepted administration action.

Red/green scenarios:

- Owner succeeds; Maintainer and unassigned principal fail without disclosure;
- slug collision and reuse fail; invalid Context/type/activation leaves state
  unchanged; removal ends access immediately;
- request retry produces one fact and one audit event;
- direct Appwrite access cannot bypass the Function.

Evidence command to add: `pnpm verify:appwrite:g3:admin`.

### D3.2 — `TASK-CONV-001` and `TASK-LIFE-001`

Build conversation first, then lifecycle:

- separate append-only `Message` and `InternalNote` records with immutable
  audience and attribution;
- Reporter and Workspace projections that cannot infer hidden-note existence;
- exact lifecycle state machine, clarification request, resolution, closure,
  reopening, reason, actor, timestamp, and history;
- optimistic concurrency/idempotency for append and transition commands.

Required BDD matrix:

- Message is visible to its allowed audience; Internal Note never reaches
  Reporter, email, cache, log, telemetry, or provider payload;
- audience conversion is impossible;
- every valid transition succeeds once; invalid or stale transitions preserve
  state and history; reopening follows the documented rule;
- response-loss retry does not duplicate a Message, Note, or transition.

Evidence command to add: `pnpm verify:appwrite:g3:conversation-lifecycle`.

### D3.3 — `TASK-WORK-001`

Build:

- scoped Maintainer inbox with Project/type/state/time/assignment filters;
- detail view with Reporter-safe content, internal evidence, attachments,
  Messages, Internal Notes, classification and lifecycle history;
- Owner/Maintainer actions backed only by trusted Function operations;
- FR/EN keyboard-complete responsive dashboard and detail layouts.

Validate assigned, unassigned, removed, sibling Project, cross-Workspace,
forged-principal, deleted, and session-revoked cases. Validate empty/loading/
retry/conflict states, non-color lifecycle meaning, accessible names, focus, and
320 px behavior.

Evidence command to add: `pnpm verify:appwrite:g3:workbench`.

### D3.4 — `TASK-NOT-001` and `TASK-NOT-002`

Extend the existing notification/outbox foundation:

- recipient policy for Reporter, Owner, assigned Maintainer, actor exclusion,
  assignment removal and lifecycle/conversation events;
- in-product feed, unread/read state, Realtime invalidation and authoritative
  refetch after missed events;
- FR/EN email templates containing only permitted public facts;
- durable provider delivery attempts, exponential retry, terminal outcome and
  reconciliation without changing the source fact.

Validate no Internal Note, Access Proof, private contact datum, attachment,
provider token, or cross-scope identifier reaches email/logs. Measure
source-to-visible P95 `<=5 s` and provider-handoff P95 `<=30 s` in the named
Preview envelope.

Evidence commands to add:

```text
pnpm verify:appwrite:g3:notifications
pnpm verify:mail:g3
```

### D3.5 — `TASK-SRC-002`

Complete rather than rebuild the existing provider foundation:

- FR/EN repository selection and connection-health UI;
- explicit refresh and disconnect;
- metadata and release import only, with provenance and ownership preservation;
- copyable Y7 badge/link resolving the current Project slug;
- no repository file mutation permission or action.

Validate unselected repositories remain absent, revoked/disconnected grants fail
closed, refresh is idempotent, slug changes preserve the badge destination, and
GitHub/GitLab results remain Workspace-scoped.

Evidence command to add: `pnpm verify:providers:g3:sources`.

### D3.6 — `TASK-ISSUE-001`

Build:

- one active Feedback-to-issue link invariant;
- assigned-actor and selected-repository checks;
- private/public repository visibility check;
- versioned Reporter consent grant/revocation for public content;
- minimal allow-listed initial issue payload and durable provider outbox.

Validate second link, unassigned actor, unselected repository, revoked grant,
public repository without consent, retry, timeout, and provider failure. Scan
payloads for Reporter identity/contact, Access Proof, Internal Note, attachment,
private Context, and unrelated Feedback.

Evidence command to add: `pnpm verify:providers:g3:issue-link`.

### Gate G3

G3 passes only when one end-to-end Preview scenario proves:

1. Reporter submits and Maintainer receives the scoped notification;
2. Maintainer requests clarification;
3. Reporter answers without seeing Internal Notes;
4. Maintainer resolves and closes;
5. Reporter reopens under the valid rule;
6. assignment removal immediately ends access;
7. notification failure preserves conversation/lifecycle facts and later
   reconciliation succeeds;
8. one Feedback links to one selected-repository issue and a second active link
   is denied;
9. all fixtures, provider grants, messages, notes, links and delivery attempts
   are cleaned without residue.

Create one evidence index and one Day 3 PR. Do not start Day 4 before G3 passes.

## Day 4 — offline, synchronization, intelligence, privacy and control

### D4.0 — Gate entry and schema migration

- rerun G1–G3 from clean Preview;
- introduce versioned schema changes for provider inbox/outbox, offline conflict
  projection, Intelligence provenance, deletion, abuse counters and exceptional
  access;
- prove forward migration, rollback/compatibility and fixture cleanup.

### D4.1 — `TASK-SYNC-001` and `TASK-SYNC-002`

This is the Day 4 critical path and starts first.

Build:

- signed webhook ingress, durable inbox claim, dedupe identity and ordering;
- provider outbox with retry, loop-prevention marker and scheduled reconcile;
- exact Y7/GitHub/GitLab lifecycle maps without inventing unsupported states;
- visible Message synchronization with provider author verification;
- revision/tombstone handling, consent enforcement and best-effort external
  cleanup after consent revocation;
- suspend/disconnect behavior that preserves Y7 as authoritative.

Validate duplicate, delayed and reordered webhook convergence; self-generated
event suppression; outage/recovery; token revocation; repository removal;
outsider/unverifiable comments; Internal Notes and protected-data payload scans;
and both provider implementations against the same contract suite.

Evidence commands to add:

```text
pnpm verify:providers:g4:state-sync
pnpm verify:providers:g4:message-sync
pnpm verify:providers:g4:reconciliation
```

### D4.2 — `TASK-PWA-001`, `TASK-OFF-001`, `TASK-OFF-002`

Build in order:

- finalize install/offline navigation/update behavior and approved cache list;
- versioned IndexedDB schema partitioned by environment, Workspace, Project,
  actor and accountless proof context;
- drafts, permitted file blobs, bounded projections and durable operation outbox;
- connectivity probe, ordered dependency replay, idempotency, Retry-After,
  exponential backoff, terminal conflict and explicit user resolution.

Validate reload survival, quota failure, logout/scope erasure, language switch,
service-worker upgrade, no protected Cache Storage, one-time synchronization,
429 pause, response-loss retry, dependency ordering, conflicting server update,
and no automatic overwrite.

Evidence commands to add:

```text
pnpm verify:pwa:g4
pnpm test:e2e:offline
```

### D4.3 — `TASK-INT-001` and `TASK-INT-002`

Build:

- trusted scoped filters and aggregates by Project, type, state, time, Reporter,
  version, place, feature and reviewed Context;
- trends with explicit window/baseline and empty-state semantics;
- attributable Themes, Feedback associations, relationships and provenance;
- correction/removal without source rewriting;
- FR/EN accessible Intelligence screens at 320 px.

Validate expected deterministic datasets, cross-Workspace and cross-Project
denial, deleted-data exclusion, theme provenance, correction attribution,
relationship scope, pagination and bounded-query performance.

Evidence command to add: `pnpm verify:appwrite:g4:intelligence`.

### D4.4 — `TASK-PRIV-001`

Build:

- authenticated or proof-authorized deletion request;
- immediate soft deletion, Reporter anonymization where required, proof and
  provider-consent revocation, notification and projection removal;
- bounded restore before expiry;
- hourly idempotent physical purge after the clock-controlled 30-day boundary;
- attachment/provider/offline/Intelligence cleanup and immutable minimal audit.

Validate ordinary absence, identity-search denial, sibling/cross-scope denial,
restore-before-boundary, purge-at-boundary, repeated workers, partial failure,
and no identity resurrection from provider sync, backup or offline replay.

Evidence command to add: `pnpm verify:appwrite:g4:privacy`.

### D4.5 — `TASK-ABUSE-001`

Build expiring HMAC counters for normalized source IP and issuer-scoped external
identity without permanent raw-IP history. Enforce all four normative limits at
trusted Function ingress.

Validate safe `429` and correct `Retry-After` at the 61st, 11th, 21st and 31st
boundaries; independent dimensions; window rollover; key rotation; IPv4/IPv6
normalization; concurrency; storage failure; cache/log/response scans.

Evidence command to add: `pnpm verify:appwrite:g4:abuse`.

### D4.6 — `TASK-PLAT-001`

Build grant request, independent approval, bounded use, revoke, expiry,
immutable audit and critical-only break-glass review. Exceptional access must
use the same trusted scope derivation and may not mutate its own audit.

Validate self-approval, same-person dual role, duration over one hour,
extension, scope breach, use before approval/after expiry, noncritical
break-glass, audit mutation, missing second principal and concurrent revoke/use.

Evidence command to add: `pnpm verify:appwrite:g4:platform-access`.

### D4.7 — begin `TASK-REC-001`

- provision the approved encrypted backup destination with least privilege;
- define daily snapshot, retention/expiry, integrity manifest and restore job;
- ensure deletion events are replayable after restore;
- write the operator runbook without embedding credentials.

The full timed recovery exercise closes on Day 5.

### Gate G4

G4 passes only when deployed evidence proves:

- offline operations synchronize exactly once and conflicts never overwrite;
- provider duplicates/reordering converge exactly once in both directions;
- outsider/unverifiable content and unconsented public content fail closed;
- Intelligence is scoped and excludes deleted facts;
- deletion disappears immediately, restore is bounded, 30-day purge is
  irreversible, and backup restore cannot resurrect deleted identity/content;
- every abuse boundary returns safe 429 behavior;
- Platform Operator cannot self-approve, exceed one hour, escape scope or alter
  the audit;
- fixture, outbox, inbox, counter, grant, backup-test and provider cleanup leaves
  no prohibited residue.

## Day 5 — verification and release

### D5.1 — finish `TASK-REC-001`

Run a timed isolated restore with non-sensitive fixtures:

- measure and prove RPO `<=24 h` and RTO `<=4 h`;
- restore application data and private files;
- replay deletions before exposing the restored environment;
- prove deleted content remains unavailable;
- prove backup expiry at the clock-controlled boundary;
- destroy the isolated recovery environment after evidence collection.

Evidence command to add: `pnpm verify:recovery:g5`.

### D5.2 — `TASK-SLO-001`

Freeze measurement definitions and the named load envelope, then prove:

- server latency histograms, provider/email handoff, RUM and synthetic uptime;
- alert routing and redacted payloads;
- load-tested SLO-005..010 thresholds;
- monthly reporting configuration without falsely claiming a month of history.

Produce a reproducible load report, alert test and measurement-series index.
Evidence command to add: `pnpm verify:slo:g5`.

### D5.3 — `TASK-E2E-001`

Create a traceability-generated end-to-end pack covering UC-01..12 and
ERR-001..019 across Reporter, Owner, Maintainer, Platform Operator, GitHub,
GitLab, offline, email, security, privacy and recovery behavior.

Every scenario must name requirement IDs, fixture owner, environment, positive
outcome, negative oracle and cleanup oracle. Run FR/EN, keyboard, accessible
names, non-color meaning and 320 px on every affected core journey.

Evidence command to add: `pnpm verify:e2e:g5`.

### D5.4 — `TASK-SEC-001` and `TASK-UX-001` final regression

- repeat all cross-Workspace/Project/environment/audience matrices;
- scan source, build, responses, logs, caches, telemetry, email and provider
  bundles for prohibited data;
- test secrets/grant/proof rotation and revocation;
- run automated accessibility and manual core-flow evidence;
- verify no high-severity security, data-loss, isolation or accessibility defect.

### D5.5 — `TASK-REL-001`

Before enabling Production behavior:

- deploy a permanent Production antivirus scanner with a new non-disclosed key,
  monitoring and documented rotation;
- configure production email, backup, provider callback/webhook and alert
  authorities with least privilege;
- audit Preview/Production mutual exclusion and bundle contents;
- provision/migrate Production, deploy Function and PWA, then run smoke;
- rehearse rollback of web, Function and compatible schema changes;
- verify Appwrite remains authoritative and destructive rollback cannot
  resurrect deleted data;
- assemble the evidence index linking every task and requirement.

Release only if G1–G5 and every task are `DONE`.

### D5.6 — `TASK-GHDP-001`

This remains `BLOCKED` until:

- the production GitHub integration has real API use;
- the public support contact is reachable;
- Production and rollback smoke pass;
- the publisher-owned GitHub App metadata and privacy/support links are final.

Then submit the GitHub Developer Program application and retain only the
submission receipt. Do not claim acceptance or a badge before GitHub grants it.

### Gate G5

The release goal is complete only when:

1. every Day 3–5 task is `DONE`;
2. every normative requirement has linked evidence;
3. production smoke and rollback pass;
4. no high-severity security, isolation, data-loss or accessibility issue is
   open;
5. permanent Production dependencies are healthy and monitored;
6. all temporary verification resources and secrets are removed;
7. the release evidence index and incomplete-items report agree.

If any criterion fails, the release result is `BLOCKED` or `IN_PROGRESS`; the
scope and definition of done are not reduced.

## Immediate execution queue after plan approval

1. Finish and integrate the Day 1/2 evidence commit.
2. Create `task/DAY-3-G3` from updated `main`.
3. Add the Day 3 schema migration contract and rollback test.
4. Execute `TASK-ADMIN-001` red-to-green.
5. Execute `TASK-CONV-001` and `TASK-LIFE-001` red-to-green.
6. Build `TASK-WORK-001`, then notification and provider UI slices.
7. Close `TASK-ISSUE-001` and execute the deployed G3 matrix.
8. Open the single Day 3 Gate PR only after the local and Preview evidence
   index is complete.

